<!-- Generated by scripts/api-docs.mjs. Do not edit. -->

# geoscratch/geo API Reference

Public symbols: 286.

## `packages/geoscratch/src/geo/coordinate-domain.ts`

### `AuxiliaryAxis`

Kind: `Type Alias`.

```ts
type AuxiliaryAxis = Readonly<{ name: string; unit: string }>
```

### `CoordinateAxis`

Kind: `Type Alias`.

```ts
type CoordinateAxis = Readonly<{ name: string; unit: string }>
```

### `CoordinateDimension`

Kind: `Type Alias`.

```ts
type CoordinateDimension = 1 | 2 | 3
```

### `coordinateDomain`

Kind: `Function`.

Validates and freezes a coordinate domain independently from storage or projection policy.

```ts
Function coordinateDomain
```

```ts
coordinateDomain(descriptor: CoordinateDomainDescriptor): CoordinateDomain
```

### `CoordinateDomain`

Kind: `Type Alias`.

```ts
type CoordinateDomain = Readonly<{ auxiliaryAxes: readonly AuxiliaryAxis[]; axes: readonly CoordinateAxis[]; embeddingAxes: readonly CoordinateAxis[]; embeddingDimensions: CoordinateDimension; id: string; intrinsicDimensions: CoordinateDimension; kind: "coordinate-domain" }>
```

### `CoordinateDomainDescriptor`

Kind: `Type Alias`.

```ts
type CoordinateDomainDescriptor = Readonly<{ auxiliaryAxes?: readonly AuxiliaryAxis[]; axes: readonly CoordinateAxis[]; embeddingAxes?: readonly CoordinateAxis[]; embeddingDimensions: CoordinateDimension; id: string; intrinsicDimensions: CoordinateDimension }>
```

### `localVector`

Kind: `Function`.

Creates a translation-invariant vector with explicit unit and basis in one domain.

```ts
Function localVector
```

```ts
localVector(domain: CoordinateDomain, values: readonly number[], options: LocalVectorOptions): LocalVector
```

### `LocalVector`

Kind: `Type Alias`.

```ts
type LocalVector = Readonly<{ basis: string; dimensions: CoordinateDimension; domainId: string; kind: "local-vector"; unit: string; values: readonly number[] }>
```

### `LocalVectorOptions`

Kind: `Type Alias`.

```ts
type LocalVectorOptions = Readonly<{ basis: string; unit: string }>
```

### `surfaceDomain`

Kind: `Function`.

Creates a two-dimensional surface domain embedded in three-dimensional space.

```ts
Function surfaceDomain
```

```ts
surfaceDomain(descriptor: SurfaceDomainDescriptor): CoordinateDomain
```

### `SurfaceDomainDescriptor`

Kind: `Type Alias`.

```ts
type SurfaceDomainDescriptor = Readonly<{ auxiliaryAxes?: readonly AuxiliaryAxis[]; axes: readonly [CoordinateAxis, CoordinateAxis]; embeddingAxes: readonly [CoordinateAxis, CoordinateAxis, CoordinateAxis]; id: string }>
```

## `packages/geoscratch/src/geo/diagnostics.ts`

### `createGeoDiagnostic`

Kind: `Function`.

Creates an immutable machine-readable diagnostic for geographic contracts.

```ts
Function createGeoDiagnostic
```

```ts
createGeoDiagnostic(input: GeoDiagnosticInput): GeoDiagnostic
```

### `GeoDiagnostic`

Kind: `Type Alias`.

```ts
type GeoDiagnostic = Readonly<{ actual?: unknown; code: string; evidence?: readonly Readonly<{ kind: string; value?: unknown }>[]; expected?: unknown; message: string; phase: GeoDiagnosticPhase; related?: readonly GeoDiagnosticSubject[]; severity: GeoDiagnosticSeverity; subject: GeoDiagnosticSubject; version: 1 }>
```

### `GeoDiagnosticError`

Kind: `Class`.

Carries one immutable Geo diagnostic through exception control flow.

```ts
class GeoDiagnosticError
```

Members:

- `cause`: `cause: unknown` (external, inherited, optional)
- `constructor`: `Constructor constructor`
  - `constructor(diagnostic: GeoDiagnostic, options?: ErrorOptions): GeoDiagnosticError`
- `diagnostic`: `diagnostic: GeoDiagnostic` (readonly)
- `message`: `message: string` (external, inherited)
- `name`: `name: string` (external, inherited)
- `stack`: `stack: string` (external, inherited, optional)

### `GeoDiagnosticInput`

Kind: `Type Alias`.

```ts
type GeoDiagnosticInput = Readonly<{ actual?: unknown; code: string; evidence?: readonly Readonly<{ kind: string; value?: unknown }>[]; expected?: unknown; message?: string; phase: GeoDiagnosticPhase; related?: readonly GeoDiagnosticSubject[]; severity?: GeoDiagnosticSeverity; subject: GeoDiagnosticSubject }>
```

### `GeoDiagnosticPhase`

Kind: `Type Alias`.

```ts
type GeoDiagnosticPhase = "coordinate" | "transform" | "virtual-raster" | "source" | "cache" | "demand" | "residency" | "sampling" | "selection"
```

### `GeoDiagnosticSeverity`

Kind: `Type Alias`.

```ts
type GeoDiagnosticSeverity = "info" | "warn" | "error"
```

### `GeoDiagnosticSubject`

Kind: `Type Alias`.

```ts
type GeoDiagnosticSubject = Readonly<{ id?: string; kind: string; label?: string; [key: string]: unknown }>
```

### `isGeoDiagnosticError`

Kind: `Function`.

Recognizes genuine Geo diagnostic errors without trusting structural lookalikes.

```ts
Function isGeoDiagnosticError
```

```ts
isGeoDiagnosticError(value: unknown): value is GeoDiagnosticError
```

## `packages/geoscratch/src/geo/frame-controller.ts`

### `createGeoFrameController`

Kind: `Function`.

Coordinates immediate submission, bounded native frames in flight, and Geo follow-up work.

```ts
Function createGeoFrameController
```

```ts
createGeoFrameController<Value, Capture = undefined>(descriptor: GeoFrameControllerDescriptor<Value, Capture>): GeoFrameController
```

### `GeoFrameCapture`

Kind: `Type Alias`.

Immutable host state captured synchronously with a monotonically increasing revision.

```ts
type GeoFrameCapture<Snapshot> = Readonly<{ revision: number; snapshot: Snapshot }>
```

### `GeoFrameController`

Kind: `Type Alias`.

Latest-only Geo frame authority with optional host-revision capture.

```ts
type GeoFrameController = Readonly<{ invalidate: any; invalidateNow: any; snapshot: any; stop: any }>
```

### `GeoFrameControllerDescriptor`

Kind: `Type Alias`.

Host capture, frame construction, observation, and bounded convergence policy.

```ts
type GeoFrameControllerDescriptor<Value, Capture = undefined> = Readonly<{ driver?: GeoFrameDriver<Capture>; maximumFollowUpFrames?: number; maximumInFlightFrames?: number; scheduler?: GeoFrameScheduler; capture?: any; onError?: any; onObserved?: any; onSubmitted?: any; render: any; track?: any }>
```

### `GeoFrameControllerFrame`

Kind: `Type Alias`.

```ts
type GeoFrameControllerFrame<Value> = GeoFrameResult<Value> & Readonly<{ frameNumber: number }>
```

### `GeoFrameControllerSnapshot`

Kind: `Type Alias`.

Immutable scheduling, capture, submission, and convergence counters.

```ts
type GeoFrameControllerSnapshot = Readonly<{ cancelledFrameCount: number; completedFrameCount: number; deduplicatedInvalidationCount: number; followUpFrameCount: number; inFlightFrameCount: number; invalidationCount: number; latestCaptureRevision?: number; maximumInFlightFrames: number; observedFrameCount: number; pendingTaskCount: number; rendering: boolean; scheduledFrameCount: number; state: GeoFrameControllerState; submittedCaptureRevision?: number; submittedFrameCount: number }>
```

### `GeoFrameControllerState`

Kind: `Type Alias`.

```ts
type GeoFrameControllerState = "running" | "stopped"
```

### `GeoFrameDriver`

Kind: `Type Alias`.

Supplies one revisioned capture, frame scheduler, and owned host lifecycle.

```ts
type GeoFrameDriver<Capture> = Readonly<{ id: string; kind: "geo-frame-driver"; scheduler: GeoFrameScheduler; capture: any; start: any; stop: any }>
```

### `GeoFrameResult`

Kind: `Type Alias`.

Work submitted for one Geo frame plus independently observed asynchronous outcomes.

```ts
type GeoFrameResult<Value> = Readonly<{ needsFollowUp: boolean; observation: PromiseLike<unknown>; settlement?: PromiseLike<GeoFrameSettlement>; value: Value }>
```

### `GeoFrameScheduler`

Kind: `Type Alias`.

```ts
type GeoFrameScheduler = Readonly<{ cancel: any; request: any }>
```

### `GeoFrameSettlement`

Kind: `Type Alias`.

Delayed feedback and residency facts that may request another bounded frame.

```ts
type GeoFrameSettlement = Readonly<{ needsFollowUp: boolean; residencySettlement?: PromiseLike<unknown>; residencyWorkCount: number }>
```

## `packages/geoscratch/src/geo/geo-field.ts`

### `geoField`

Kind: `Function`.

Defines immutable value, interpolation, and missing-data semantics over a coordinate domain.

```ts
Function geoField
```

```ts
geoField(descriptor: GeoFieldDescriptor): GeoField
```

### `GeoField`

Kind: `Type Alias`.

```ts
type GeoField = Readonly<{ channels: 1 | 2 | 3 | 4; domain: CoordinateDomain; fieldKind: VirtualRasterFieldKind; id: string; interpolation: GeoFieldInterpolation; kind: "geo-field"; label?: string; noData?: number; sampleType: VirtualRasterSampleType; unit?: string }>
```

### `GeoFieldDescriptor`

Kind: `Type Alias`.

```ts
type GeoFieldDescriptor = Readonly<{ channels: 1 | 2 | 3 | 4; domain: CoordinateDomain; id: string; interpolation: GeoFieldInterpolation; kind: VirtualRasterFieldKind; label?: string; noData?: number; sampleType: VirtualRasterSampleType; unit?: string }>
```

### `GeoFieldInterpolation`

Kind: `Type Alias`.

```ts
type GeoFieldInterpolation = "nearest" | "linear"
```

### `tiledFieldRepresentation`

Kind: `Function`.

Associates a Geo field with one tiled Virtual Raster plane and spatial profile.

```ts
Function tiledFieldRepresentation
```

```ts
tiledFieldRepresentation(descriptor: TiledFieldRepresentationDescriptor): TiledFieldRepresentation
```

### `TiledFieldRepresentation`

Kind: `Type Alias`.

```ts
type TiledFieldRepresentation = Readonly<{ field: GeoField; id: string; kind: "tiled-field-representation"; plane: VirtualRasterPlane; sourceRevision: string; spatialProfile: TileSpatialProfile }>
```

### `TiledFieldRepresentationDescriptor`

Kind: `Type Alias`.

```ts
type TiledFieldRepresentationDescriptor = Readonly<{ field: GeoField; id: string; plane: VirtualRasterPlane; sourceRevision: string; spatialProfile: TileSpatialProfile }>
```

## `packages/geoscratch/src/geo/geo-view.ts`

### `createGeoViewAdapter`

Kind: `Function`.

Wraps an external camera reader as a stable Geo view-snapshot producer.

```ts
Function createGeoViewAdapter
```

```ts
createGeoViewAdapter<Input>(descriptor: GeoViewAdapterDescriptor<Input>): GeoViewAdapter<Input>
```

### `createGeoViewSnapshot`

Kind: `Function`.

Freezes one revisioned camera and logical reference viewport in a relative-world frame.

```ts
Function createGeoViewSnapshot
```

```ts
createGeoViewSnapshot(descriptor: GeoViewSnapshotDescriptor): GeoViewSnapshot
```

### `createGeoViewSource`

Kind: `Function`.

Wraps an external view reader behind one validated immutable capture contract.

```ts
Function createGeoViewSource
```

```ts
createGeoViewSource<View>(descriptor: GeoViewSourceDescriptor<View>): GeoViewSource<View>
```

### `GeoViewAdapter`

Kind: `Type Alias`.

```ts
type GeoViewAdapter<Input = unknown> = Readonly<{ id: string; kind: "geo-view-adapter"; read: any }>
```

### `GeoViewAdapterDescriptor`

Kind: `Type Alias`.

```ts
type GeoViewAdapterDescriptor<Input> = Readonly<{ id: string; read: any }>
```

### `GeoViewReadContext`

Kind: `Type Alias`.

```ts
type GeoViewReadContext = Readonly<{ frameEpoch: number; residencySnapshotEpoch: number }>
```

### `GeoViewSnapshot`

Kind: `Type Alias`.

```ts
type GeoViewSnapshot = Readonly<{ cameraHigh: readonly [number, number, number]; cameraLatitudeRadians: number; cameraLow: readonly [number, number, number]; cameraPitchRadians: number; clipFromRelativeWorld: readonly number[]; frameEpoch: number; id: string; kind: "geo-view-snapshot"; referenceViewport: readonly [number, number]; residencySnapshotEpoch: number; verticalFovRadians: number; zoomHint: number }>
```

### `GeoViewSnapshotDescriptor`

Kind: `Type Alias`.

```ts
type GeoViewSnapshotDescriptor = Readonly<{ cameraHigh: readonly [number, number, number]; cameraLatitudeRadians: number; cameraLow: readonly [number, number, number]; cameraPitchRadians: number; clipFromRelativeWorld: ArrayLike<number>; frameEpoch: number; id: string; referenceViewport: readonly [number, number]; residencySnapshotEpoch: number; verticalFovRadians: number; zoomHint: number }>
```

### `GeoViewSource`

Kind: `Type Alias`.

Synchronously captures one immutable view and physical presentation size.

```ts
type GeoViewSource<View> = Readonly<{ id: string; kind: "geo-view-source"; capture: any }>
```

### `GeoViewSourceCapture`

Kind: `Type Alias`.

```ts
type GeoViewSourceCapture<View> = Readonly<{ presentationSize: Readonly<SurfaceSize>; view: View }>
```

### `GeoViewSourceDescriptor`

Kind: `Type Alias`.

```ts
type GeoViewSourceDescriptor<View> = Readonly<{ id: string; capture: any }>
```

## `packages/geoscratch/src/geo/gpu-web-mercator-quad-cover-layout.ts`

### `gpuWebMercatorQuadCoverReadWgslModule`

Kind: `Function`.

Generates standard-tile lookup, neighbor, and edge-stitching WGSL for one cover.

```ts
Function gpuWebMercatorQuadCoverReadWgslModule
```

```ts
gpuWebMercatorQuadCoverReadWgslModule(options: GpuWebMercatorQuadCoverReadWgslOptions): GpuWebMercatorQuadCoverReadWgslModule
```

### `GpuWebMercatorQuadCoverReadWgslModule`

Kind: `Type Alias`.

```ts
type GpuWebMercatorQuadCoverReadWgslModule = Readonly<{ bindings: Readonly<{ group: number; lookupEntries: number; visibleInstances: number }>; code: string; kind: "gpu-web-mercator-quad-cover-read-wgsl-module"; layoutDependencies: readonly LayoutArtifact[]; namespace: string }>
```

### `GpuWebMercatorQuadCoverReadWgslOptions`

Kind: `Type Alias`.

```ts
type GpuWebMercatorQuadCoverReadWgslOptions = Readonly<{ group: number; lookupEntriesBinding: number; namespace?: string; visibleInstancesBinding: number }>
```

## `packages/geoscratch/src/geo/gpu-web-mercator-quad-cover.ts`

### `decodeGpuWebMercatorQuadCoverFeedback`

Kind: `Function`.

Decodes and validates bounded inverse-cover state and desired-page feedback.

```ts
Function decodeGpuWebMercatorQuadCoverFeedback
```

```ts
decodeGpuWebMercatorQuadCoverFeedback(stateBytes: Uint8Array, demandBytes: Uint8Array, options: Readonly<{ expectedFrameEpoch: number; maximumPatches: number; sourceLevelCeiling: number }>): Readonly<GpuWebMercatorQuadCoverSelectionFacts & { demands: readonly GpuWebMercatorQuadCoverDemand[] }>
```

### `GpuWebMercatorQuadCover`

Kind: `Class`.

Owns the bounded GPU graph that derives one standard WebMercatorQuad view cover.

```ts
class GpuWebMercatorQuadCover
```

Members:

- `capture`: `Method capture`
  - `capture(builder: SubmissionBuilder, frame: GpuWebMercatorQuadCoverFrame): SubmissionBuilder`
- `commandsFor`: `Method commandsFor`
  - `commandsFor(frame: GpuWebMercatorQuadCoverFrame): GpuWebMercatorQuadCoverCommands`
- `create`: `Method create` (static)
  - `create(runtime: GPURuntime, input: GpuWebMercatorQuadCoverDescriptor): Promise<GpuWebMercatorQuadCover>`
- `descriptor`: `descriptor: GpuWebMercatorQuadCoverDescriptor` (readonly)
- `dispose`: `Method dispose`
  - `dispose(): void`
- `encode`: `Method encode`
  - `encode(builder: SubmissionBuilder, frame: GpuWebMercatorQuadCoverFrame): SubmissionBuilder`
- `facts`: `Method facts`
  - `facts(): GpuWebMercatorQuadCoverFacts`
- `feedback`: `Method feedback`
  - `feedback(frame: GpuWebMercatorQuadCoverFrame, submitted: SubmittedWork): Promise<GpuWebMercatorQuadCoverFeedback>`
- `frame`: `Method frame`
  - `frame(token: GpuWebMercatorQuadCoverViewToken): GpuWebMercatorQuadCoverFrame`
- `id`: `id: string` (readonly)
- `identityObjects`: `Method identityObjects`
  - `identityObjects(): GpuWebMercatorQuadCoverIdentityObjects`
- `initialize`: `Method initialize`
  - `initialize(builder: SubmissionBuilder): SubmissionBuilder`
- `renderTemplates`: `Method renderTemplates`
  - `renderTemplates(): readonly [Readonly<{ coverId: string; coverLookup: BufferResource; drawArgument: Readonly<{ offset: 0; region: BufferRegion; resource: BufferResource; size: 16 }>; mapMeta: BufferResource; parity: 0 | 1; templateId: "patch-mesh"; visibleInstances: BufferResource }>, Readonly<{ coverId: string; coverLookup: BufferResource; drawArgument: Readonly<{ offset: 0; region: BufferRegion; resource: BufferResource; size: 16 }>; mapMeta: BufferResource; parity: 0 | 1; templateId: "patch-mesh"; visibleInstances: BufferResource }>]`
- `runtime`: `runtime: GPURuntime` (readonly)
- `writeView`: `Method writeView`
  - `writeView(view: GeoViewSnapshot): GpuWebMercatorQuadCoverViewToken`

### `GpuWebMercatorQuadCoverCommands`

Kind: `Type Alias`.

```ts
type GpuWebMercatorQuadCoverCommands = Readonly<{ demandFeedback: ReadbackCommand; generate: DispatchCommand; stateFeedback: ReadbackCommand }>
```

### `GpuWebMercatorQuadCoverDemand`

Kind: `Type Alias`.

```ts
type GpuWebMercatorQuadCoverDemand = Readonly<{ decisionFrameEpoch: number; desiredSampleLevel: number; priority: number; requestMatrixLevel: number; residencySnapshotEpoch: number; sourceLevelCeiling: number; tileCol: number; tileRow: number }>
```

### `GpuWebMercatorQuadCoverDescriptor`

Kind: `Type Alias`.

```ts
type GpuWebMercatorQuadCoverDescriptor = Readonly<{ elevationBounds?: readonly WebMercatorTileElevationBounds[]; elevationRangeMeters: readonly [number, number]; policy: GpuWebMercatorQuadCoverPolicy; spatialProfile: WebMercatorPlanarTileSpatialProfile; vertexCount: number }>
```

### `GpuWebMercatorQuadCoverFacts`

Kind: `Type Alias`.

```ts
type GpuWebMercatorQuadCoverFacts = Readonly<{ coverageLimitCount: number; disposed: boolean; elevationBoundCount: number; elevationBoundsBufferId: string; elevationBoundsMode: "global" | "hierarchy"; id: string; lookupCapacity: number; parity: readonly Readonly<{ commandIds: readonly string[]; demandBufferId: string; drawArgumentBufferId: string; lookupBufferId: string; mapMetaBufferId: string; parity: 0 | 1; patchBufferId: string; stateBufferId: string }>[]; policy: GpuWebMercatorQuadCoverPolicy; runtimeId: string; selectionPath: "gpu-camera-inverse-webmercatorquad-cover" }>
```

### `GpuWebMercatorQuadCoverFeedback`

Kind: `Type Alias`.

```ts
type GpuWebMercatorQuadCoverFeedback = GpuWebMercatorQuadCoverSelectionFacts & Readonly<{ coverId: string; demands: readonly GpuWebMercatorQuadCoverDemand[]; kind: "gpu-web-mercator-quad-cover-feedback"; submissionId: string }>
```

### `GpuWebMercatorQuadCoverFrame`

Kind: `Type Alias`.

```ts
type GpuWebMercatorQuadCoverFrame = Readonly<{ coverId: string; frameEpoch: number; kind: "gpu-web-mercator-quad-cover-frame"; parity: 0 | 1; residencySnapshotEpoch: number; visibleInstances: BufferResource }>
```

### `GpuWebMercatorQuadCoverIdentityObjects`

Kind: `Type Alias`.

```ts
type GpuWebMercatorQuadCoverIdentityObjects = Readonly<{ bindLayouts: readonly BindLayout[]; bindSets: readonly BindSet[]; commands: readonly (ClearBufferCommand | DispatchCommand | ReadbackCommand)[]; passes: readonly ComputePassSpec[]; pipelines: readonly ComputePipeline[]; programs: readonly Program[]; resources: readonly BufferResource[]; uploads: readonly UploadCommand[] }>
```

### `gpuWebMercatorQuadCoverPolicy`

Kind: `Function`.

Validates immutable projected-cell quality, pitch boundary, source-ceiling, and capacity facts for one camera-derived standard WebMercatorQuad cover.

```ts
Function gpuWebMercatorQuadCoverPolicy
```

```ts
gpuWebMercatorQuadCoverPolicy(input: GpuWebMercatorQuadCoverPolicy): GpuWebMercatorQuadCoverPolicy
```

### `GpuWebMercatorQuadCoverPolicy`

Kind: `Type Alias`.

```ts
type GpuWebMercatorQuadCoverPolicy = Readonly<{ cellsPerPatchEdge: number; maximumCellSpanReferencePixels: number; maximumMatrixLevel: number; maximumPatches: number; minimumMatrixLevel: number; referenceTileSizePixels: number; refinementTolerance: number; sourceMaximumMatrixLevel: number; variableLodPitchThresholdRadians: number }>
```

### `GpuWebMercatorQuadCoverRenderTemplate`

Kind: `Type Alias`.

```ts
type GpuWebMercatorQuadCoverRenderTemplate = Readonly<{ coverId: string; coverLookup: BufferResource; drawArgument: Readonly<{ offset: 0; region: BufferRegion; resource: BufferResource; size: 16 }>; mapMeta: BufferResource; parity: 0 | 1; templateId: "patch-mesh"; visibleInstances: BufferResource }>
```

### `GpuWebMercatorQuadCoverSelectionFacts`

Kind: `Type Alias`.

```ts
type GpuWebMercatorQuadCoverSelectionFacts = Readonly<{ candidateCount: number; demandCount: number; demandOverflowCount: number; descriptorOverflowCount: number; finestMatrixLevel: number; frameEpoch: number; lookupOverflowCount: number; maximumAdjacentLevelDelta: number; maximumCellSpanReferencePixels?: number; maximumMatrixLevel?: number; minimumCellSpanReferencePixels?: number; minimumMatrixLevel?: number; patchCount: number; selectionMode: "uniform" | "variable"; sourceLevelCeiling: number }>
```

### `GpuWebMercatorQuadCoverViewToken`

Kind: `Type Alias`.

```ts
type GpuWebMercatorQuadCoverViewToken = Readonly<{ coverId: string; frameEpoch: number; isDisposed: boolean; kind: "gpu-web-mercator-quad-cover-view-token"; parity: 0 | 1; residencySnapshotEpoch: number; dispose: any }>
```

### `WebMercatorTileElevationBounds`

Kind: `Type Alias`.

Immutable source-metadata elevation range for one standard WebMercatorQuad tile.

```ts
type WebMercatorTileElevationBounds = Readonly<{ matrixLevel: number; maximumElevationMeters: number; minimumElevationMeters: number; tileCol: number; tileRow: number }>
```

## `packages/geoscratch/src/geo/map-field-layer.ts`

### `mapFieldLayer`

Kind: `Function`.

Composes a planar field representation, view adapter, and demand producer without owning them.

```ts
Function mapFieldLayer
```

```ts
mapFieldLayer<Input = unknown>(descriptor: MapFieldLayerDescriptor<Input>): MapFieldLayer<Input>
```

### `MapFieldLayer`

Kind: `Type Alias`.

```ts
type MapFieldLayer<Input = unknown> = Readonly<{ demandProducer: ViewDemandProducer; field: GeoField; id: string; kind: "map-field-layer"; representation: TiledFieldRepresentation; spatialProfile: TileSpatialProfile; viewAdapter: GeoViewAdapter<Input> }>
```

### `MapFieldLayerDescriptor`

Kind: `Type Alias`.

```ts
type MapFieldLayerDescriptor<Input = unknown> = Readonly<{ demandProducer: ViewDemandProducer; field: GeoField; id: string; representation: TiledFieldRepresentation; spatialProfile: TileSpatialProfile; viewAdapter: GeoViewAdapter<Input> }>
```

## `packages/geoscratch/src/geo/maplibre-frame-driver.ts`

### `mapLibreFrameDriver`

Kind: `Function`.

Creates a no-draw MapLibre custom-layer driver with revisioned host capture.

```ts
Function mapLibreFrameDriver
```

```ts
mapLibreFrameDriver<Capture>(descriptor: MapLibreFrameDriverDescriptor<Capture>): MapLibreFrameDriver<Capture>
```

### `MapLibreFrameDriver`

Kind: `Type Alias`.

Geo frame driver synchronized to a MapLibre-compatible custom-layer callback.

```ts
type MapLibreFrameDriver<Capture> = GeoFrameDriver<Capture>
```

### `MapLibreFrameDriverDescriptor`

Kind: `Type Alias`.

Map host, stable layer id, and application capture read by a MapLibre driver.

```ts
type MapLibreFrameDriverDescriptor<Capture> = Readonly<{ id: string; map: MapLibreFrameMap; capture: any }>
```

### `MapLibreFrameLayer`

Kind: `Type Alias`.

No-draw custom-layer shape used only to enter a MapLibre render frame.

```ts
type MapLibreFrameLayer = Readonly<{ id: string; renderingMode: "2d"; type: "custom"; render: any }>
```

### `MapLibreFrameMap`

Kind: `Type Alias`.

MapLibre-compatible host methods required by the frame driver.

```ts
type MapLibreFrameMap = Readonly<{ addLayer: any; getLayer: any; off: any; on: any; removeLayer: any; triggerRepaint: any }>
```

## `packages/geoscratch/src/geo/maplibre-planar-view.ts`

### `MapLibreLngLat`

Kind: `Type Alias`.

```ts
type MapLibreLngLat = Readonly<{ lat: number; lng: number }>
```

### `MapLibreMercatorCoordinate`

Kind: `Type Alias`.

```ts
type MapLibreMercatorCoordinate = Readonly<{ x: number; y: number; z: number }>
```

### `MapLibrePlanarCameraInput`

Kind: `Type Alias`.

```ts
type MapLibrePlanarCameraInput = Readonly<{ map: MapLibrePlanarMap; minimumElevationMeters: number; referenceViewport: MapLibrePlanarViewport }>
```

### `MapLibrePlanarCameraState`

Kind: `Type Alias`.

```ts
type MapLibrePlanarCameraState = Omit<GeoViewSnapshotDescriptor, "id" | "frameEpoch" | "residencySnapshotEpoch"> & Readonly<{ bearingDegrees: number; center: readonly [number, number]; far: number; near: number; pitchDegrees: number }>
```

### `MapLibrePlanarMap`

Kind: `Type Alias`.

```ts
type MapLibrePlanarMap = Readonly<{ transform: MapLibrePlanarTransform; getBearing: any; getCenter: any; getPitch: any; getZoom: any }>
```

### `MapLibrePlanarTransform`

Kind: `Type Alias`.

```ts
type MapLibrePlanarTransform = Readonly<{ _elevation?: number; _fov?: number; _pitch?: number; _pixelPerMeter?: number; angle: number; bearing: number; cameraToCenterDistance?: number; center: Readonly<{ lat: number }>; centerOffset?: MapLibrePoint | null; elevation?: number; farZ: number; fov: number; height: number; mercatorMatrix?: ArrayLike<number> | null; minElevationForCurrentTile?: number; nearZ: number; pitch: number; pixelsPerMeter?: number; point: MapLibrePoint; width: number; worldSize: number; getCameraPosition: any; getHorizon?: any }>
```

### `mapLibrePlanarViewAdapter`

Kind: `Function`.

Adapts MapLibre-compatible camera state into precision-preserving planar Geo snapshots.

```ts
Function mapLibrePlanarViewAdapter
```

```ts
mapLibrePlanarViewAdapter(descriptor: MapLibrePlanarViewAdapterDescriptor): MapLibrePlanarViewAdapter
```

### `MapLibrePlanarViewAdapter`

Kind: `Type Alias`.

```ts
type MapLibrePlanarViewAdapter = GeoViewAdapter<MapLibrePlanarCameraState> & Readonly<{ viewId: string; camera: any }>
```

### `MapLibrePlanarViewAdapterDescriptor`

Kind: `Type Alias`.

```ts
type MapLibrePlanarViewAdapterDescriptor = Readonly<{ id: string; viewId?: string; mercatorCoordinateFromLngLat: any }>
```

### `MapLibrePlanarViewport`

Kind: `Type Alias`.

```ts
type MapLibrePlanarViewport = Readonly<{ height: number; width: number }>
```

### `mapLibrePlanarViewSource`

Kind: `Function`.

Composes a MapLibre host and planar adapter into one immutable Geo view source.

```ts
Function mapLibrePlanarViewSource
```

```ts
mapLibrePlanarViewSource(descriptor: MapLibrePlanarViewSourceDescriptor): MapLibrePlanarViewSource
```

### `MapLibrePlanarViewSource`

Kind: `Type Alias`.

Captures one MapLibre planar camera and physical presentation size without host ownership.

```ts
type MapLibrePlanarViewSource = GeoViewSource<MapLibrePlanarCameraState>
```

### `MapLibrePlanarViewSourceDescriptor`

Kind: `Type Alias`.

```ts
type MapLibrePlanarViewSourceDescriptor = Readonly<{ adapter: MapLibrePlanarViewAdapter; id: string; map: MapLibrePlanarMap; minimumElevationMeters: number; presentationSize: any }>
```

## `packages/geoscratch/src/geo/mercatorCoordinate.ts`

### `MercatorCoordinate`

Kind: `Class`.

Provides stateless longitude/latitude conversion to normalized Web Mercator coordinates.

```ts
class MercatorCoordinate
```

Members:

- `constructor`: `Constructor constructor`
  - `constructor(): MercatorCoordinate`
- `fromLonLat`: `Method fromLonLat` (static)
  - `fromLonLat(lonLat: readonly [number, number]): [number, number]`
- `fromNDC`: `Method fromNDC` (static)
  - `fromNDC(xy: readonly [number, number]): [number, number]`
- `fromXY`: `Method fromXY` (static)
  - `fromXY(xy: readonly [number, number]): [number, number]`
- `latFromMercatorY`: `Method latFromMercatorY` (static)
  - `latFromMercatorY(y: number): number`
- `lonFromMercatorX`: `Method lonFromMercatorX` (static)
  - `lonFromMercatorX(x: number): number`
- `mercatorXfromLon`: `Method mercatorXfromLon` (static)
  - `mercatorXfromLon(lon: number): number`
- `mercatorYfromLat`: `Method mercatorYfromLat` (static)
  - `mercatorYfromLat(lat: number): number`
- `toNDC`: `Method toNDC` (static)
  - `toNDC(coords: readonly [number, number]): [number, number]`

## `packages/geoscratch/src/geo/position-codec.ts`

### `cellLocalF32Codec`

Kind: `Function`.

Creates a cell-local f32 codec for one coordinate domain.

```ts
Function cellLocalF32Codec
```

```ts
cellLocalF32Codec(options: CellLocalF32CodecOptions): CellLocalF32Codec
```

### `CellLocalF32Codec`

Kind: `Class`.

Encodes positions as integer cells plus bounded f32 locals for stable dynamic shader work.

```ts
class CellLocalF32Codec
```

Members:

- `advance`: `Method advance`
  - `advance(position: CellLocalPosition, displacement: LocalVector): CellLocalPosition`
- `assertPrecisionBudget`: `Method assertPrecisionBudget`
  - `assertPrecisionBudget(maximumUlp: number): void`
- `cameraRelative`: `Method cameraRelative`
  - `cameraRelative(position: CellLocalPosition, camera: CellLocalPosition): number[]`
- `constructor`: `Constructor constructor`
  - `constructor(options: CellLocalF32CodecOptions): CellLocalF32Codec`
- `difference`: `Method difference`
  - `difference(position: CellLocalPosition, origin: CellLocalPosition): number[]`
- `domain`: `domain: CoordinateDomain` (readonly)
- `facts`: `facts: PositionPrecisionFacts` (readonly)
- `normalize`: `Method normalize`
  - `normalize(input: CellLocalPositionInput): CellLocalPosition`
- `pack`: `Method pack`
  - `pack(positions: readonly Readonly<{ cells: readonly number[]; dimensions: CoordinateDimension; encoding: "cell-local-f32"; local: readonly number[] }>[]): Uint8Array`
- `rebase`: `Method rebase`
  - `rebase(position: CellLocalPosition, origin: CellLocalPosition): CellLocalPosition`
- `unpack`: `Method unpack`
  - `unpack(bytes: ArrayBuffer | ArrayBufferView<ArrayBufferLike>): Readonly<{ cells: readonly number[]; dimensions: CoordinateDimension; encoding: "cell-local-f32"; local: readonly number[] }>[]`
- `wgslModule`: `Method wgslModule`
  - `wgslModule(options: PositionWgslOptions = {}): string`

### `CellLocalF32CodecOptions`

Kind: `Type Alias`.

```ts
type CellLocalF32CodecOptions = Readonly<{ cellExtent: number | readonly number[]; domain: CoordinateDomain; maxLocalUlp?: number; wrapPeriodCells?: number | readonly number[] }>
```

### `CellLocalPosition`

Kind: `Type Alias`.

```ts
type CellLocalPosition = Readonly<{ cells: readonly number[]; dimensions: CoordinateDimension; encoding: "cell-local-f32"; local: readonly number[] }>
```

### `CellLocalPositionInput`

Kind: `Type Alias`.

```ts
type CellLocalPositionInput = Readonly<{ cells: readonly number[]; local: readonly number[] }>
```

### `CoordinateOverflowPolicy`

Kind: `Type Alias`.

```ts
type CoordinateOverflowPolicy = "error"
```

### `CoordinateWrapPolicy`

Kind: `Type Alias`.

```ts
type CoordinateWrapPolicy = "none" | "periodic"
```

### `PositionEncoding`

Kind: `Type Alias`.

```ts
type PositionEncoding = "cell-local-f32" | "wide-fixed"
```

### `PositionPrecisionFacts`

Kind: `Type Alias`.

```ts
type PositionPrecisionFacts = Readonly<{ bytesPerPosition: number; cellExtent?: readonly number[]; dimensions: CoordinateDimension; encoding: PositionEncoding; fixedQuantum?: number; maxLocalUlp: number; overflowPolicy: CoordinateOverflowPolicy; quantizationError: number; representableRange: Readonly<{ maximum: string; minimum: string }>; supportedOperations: readonly string[]; wrapPolicy: CoordinateWrapPolicy }>
```

### `PositionWgslOptions`

Kind: `Type Alias`.

```ts
type PositionWgslOptions = Readonly<{ namespace?: string }>
```

### `WideFixedAxis`

Kind: `Type Alias`.

```ts
type WideFixedAxis = Readonly<{ high: number; low: number }>
```

### `wideFixedCodec`

Kind: `Function`.

Creates a wide fixed-point codec for one coordinate domain and quantum.

```ts
Function wideFixedCodec
```

```ts
wideFixedCodec(options: WideFixedCodecOptions): WideFixedCodec
```

### `WideFixedCodec`

Kind: `Class`.

Quantizes canonical positions into signed 64-bit axes represented by portable word pairs.

```ts
class WideFixedCodec
```

Members:

- `add`: `Method add`
  - `add(position: WideFixedPosition, delta: readonly bigint[]): WideFixedPosition`
- `constructor`: `Constructor constructor`
  - `constructor(options: WideFixedCodecOptions): WideFixedCodec`
- `decomposeLod`: `Method decomposeLod`
  - `decomposeLod(position: WideFixedPosition, options: Readonly<{ lod: number; pageSize: number }>): readonly Readonly<{ page: bigint; subTexel: bigint; texel: number }>[]`
- `difference`: `Method difference`
  - `difference(position: WideFixedPosition, origin: WideFixedPosition): bigint[]`
- `domain`: `domain: CoordinateDomain` (readonly)
- `facts`: `facts: PositionPrecisionFacts` (readonly)
- `fromQuanta`: `Method fromQuanta`
  - `fromQuanta(values: readonly bigint[]): WideFixedPosition`
- `pack`: `Method pack`
  - `pack(positions: readonly Readonly<{ dimensions: CoordinateDimension; encoding: "wide-fixed"; limbs: readonly Readonly<{ high: number; low: number }>[] }>[]): Uint8Array`
- `subtract`: `Method subtract`
  - `subtract(position: WideFixedPosition, delta: readonly bigint[]): WideFixedPosition`
- `toQuanta`: `Method toQuanta`
  - `toQuanta(position: WideFixedPosition): bigint[]`
- `unpack`: `Method unpack`
  - `unpack(bytes: ArrayBuffer | ArrayBufferView<ArrayBufferLike>): Readonly<{ dimensions: CoordinateDimension; encoding: "wide-fixed"; limbs: readonly Readonly<{ high: number; low: number }>[] }>[]`
- `wgslModule`: `Method wgslModule`
  - `wgslModule(options: PositionWgslOptions = {}): string`

### `WideFixedCodecOptions`

Kind: `Type Alias`.

```ts
type WideFixedCodecOptions = Readonly<{ domain: CoordinateDomain; quantum: number }>
```

### `WideFixedLodAddress`

Kind: `Type Alias`.

```ts
type WideFixedLodAddress = Readonly<{ page: bigint; subTexel: bigint; texel: number }>
```

### `WideFixedPosition`

Kind: `Type Alias`.

```ts
type WideFixedPosition = Readonly<{ dimensions: CoordinateDimension; encoding: "wide-fixed"; limbs: readonly WideFixedAxis[] }>
```

## `packages/geoscratch/src/geo/tile-matrix.ts`

### `TileCoordinate`

Kind: `Type Alias`.

```ts
type TileCoordinate = Readonly<{ key: string; kind: "tile-coordinate"; matrixId: TileMatrixId; tileCol: number; tileMatrixSetId: string; tileRow: number }>
```

### `TileCoordinateDescriptor`

Kind: `Type Alias`.

```ts
type TileCoordinateDescriptor = Readonly<{ matrixId: TileMatrixId; tileCol: number; tileRow: number }>
```

### `TileMatrix`

Kind: `Type Alias`.

```ts
type TileMatrix = Readonly<TileMatrixDescriptor>
```

### `TileMatrixCornerOfOrigin`

Kind: `Type Alias`.

```ts
type TileMatrixCornerOfOrigin = "topLeft" | "bottomLeft"
```

### `tileMatrixCoverage`

Kind: `Function`.

Creates finite source coverage over an existing tile matrix set.

```ts
Function tileMatrixCoverage
```

```ts
tileMatrixCoverage(descriptor: TileMatrixCoverageDescriptor): TileMatrixCoverage
```

### `TileMatrixCoverage`

Kind: `Class`.

Indexes finite per-level TileMatrix limits into compact logical and GPU addresses.

```ts
class TileMatrixCoverage
```

Members:

- `constructor`: `Constructor constructor`
  - `constructor(descriptor: TileMatrixCoverageDescriptor): TileMatrixCoverage`
- `contains`: `Method contains`
  - `contains(descriptor: TileCoordinateDescriptor): boolean`
- `coordinate`: `Method coordinate`
  - `coordinate(index: number): TileCoordinate`
- `entryCount`: `entryCount: number` (readonly)
- `index`: `Method index`
  - `index(descriptor: TileCoordinateDescriptor): number`
- `kind`: `kind: "tile-matrix-coverage"` (readonly)
- `limit`: `Method limit`
  - `limit(matrixId: string): Readonly<{ matrixId: string; maxTileCol: number; maxTileRow: number; minTileCol: number; minTileRow: number }> | undefined`
- `limits`: `limits: readonly Readonly<{ matrixId: string; maxTileCol: number; maxTileRow: number; minTileCol: number; minTileRow: number }>[]` (readonly)
- `parent`: `Method parent`
  - `parent(descriptor: TileCoordinateDescriptor): Readonly<{ key: string; kind: "tile-coordinate"; matrixId: string; tileCol: number; tileMatrixSetId: string; tileRow: number }> | undefined`
- `tileMatrixSet`: `tileMatrixSet: TileMatrixSet` (readonly)
- `tryIndex`: `Method tryIndex`
  - `tryIndex(descriptor: TileCoordinateDescriptor): number | undefined`
- `wgslModule`: `Method wgslModule`
  - `wgslModule(options: TileMatrixCoverageWgslOptions = {}): string`

### `TileMatrixCoverageDescriptor`

Kind: `Type Alias`.

```ts
type TileMatrixCoverageDescriptor = Readonly<{ limits: readonly TileMatrixLimits[]; tileMatrixSet: TileMatrixSet }>
```

### `TileMatrixCoverageWgslOptions`

Kind: `Type Alias`.

```ts
type TileMatrixCoverageWgslOptions = Readonly<{ namespace?: string }>
```

### `TileMatrixDescriptor`

Kind: `Type Alias`.

```ts
type TileMatrixDescriptor = Readonly<{ cellSize: number; cornerOfOrigin: TileMatrixCornerOfOrigin; id: TileMatrixId; matrixHeight: number; matrixWidth: number; pointOfOrigin: readonly [number, number]; scaleDenominator: number; tileHeight: number; tileWidth: number }>
```

### `TileMatrixId`

Kind: `Type Alias`.

```ts
type TileMatrixId = string
```

### `TileMatrixLimits`

Kind: `Type Alias`.

```ts
type TileMatrixLimits = Readonly<{ matrixId: TileMatrixId; maxTileCol: number; maxTileRow: number; minTileCol: number; minTileRow: number }>
```

### `tileMatrixSet`

Kind: `Function`.

Validates and freezes an OGC-style ordered tile matrix set.

```ts
Function tileMatrixSet
```

```ts
tileMatrixSet(descriptor: TileMatrixSetDescriptor): TileMatrixSet
```

### `TileMatrixSet`

Kind: `Type Alias`.

```ts
type TileMatrixSet = Readonly<{ boundingBox: TileMatrixSetBoundingBox; crs: string; id: string; kind: "tile-matrix-set"; orderedAxes: readonly [string, string]; tileMatrices: readonly TileMatrix[]; title?: string; uri?: string; wellKnownScaleSet?: string; matrix: any; parent: any; tile: any }>
```

### `TileMatrixSetBoundingBox`

Kind: `Type Alias`.

```ts
type TileMatrixSetBoundingBox = Readonly<{ crs: string; lowerCorner: readonly [number, number]; upperCorner: readonly [number, number] }>
```

### `TileMatrixSetDescriptor`

Kind: `Type Alias`.

```ts
type TileMatrixSetDescriptor = Readonly<{ boundingBox: TileMatrixSetBoundingBox; crs: string; id: string; orderedAxes: readonly [string, string]; tileMatrices: readonly TileMatrixDescriptor[]; title?: string; uri?: string; wellKnownScaleSet?: string }>
```

## `packages/geoscratch/src/geo/tile-spatial-profile.ts`

### `PlanarTileBounds`

Kind: `Type Alias`.

```ts
type PlanarTileBounds = Readonly<{ east: number; north: number; south: number; west: number }>
```

### `planarTileSpatialProfile`

Kind: `Function`.

Defines planar bounds, camera encoding, and fixed coordinates for one tiled topology.

```ts
Function planarTileSpatialProfile
```

```ts
planarTileSpatialProfile(descriptor: PlanarTileSpatialProfileDescriptor): TileSpatialProfile
```

### `PlanarTileSpatialProfileDescriptor`

Kind: `Type Alias`.

```ts
type PlanarTileSpatialProfileDescriptor = Readonly<{ coordinateBits?: number; coverage: TileMatrixCoverage; id: string; topology: TileTopology; wrapX?: boolean }>
```

### `TileSpatialCameraEncoding`

Kind: `Type Alias`.

```ts
type TileSpatialCameraEncoding = Readonly<{ high: readonly [number, number]; low: readonly [number, number] }>
```

### `TileSpatialFixedEncoding`

Kind: `Type Alias`.

```ts
type TileSpatialFixedEncoding = Readonly<{ coordinateBits: number; highLimbMeters: readonly [number, number]; quantumMeters: readonly [number, number]; rootColumnBits: number; rootRowBits: number }>
```

### `TileSpatialProfile`

Kind: `Type Alias`.

```ts
type TileSpatialProfile = Readonly<{ coordinateBits: number; coordinateFrame: "planar"; coverage: TileMatrixCoverage; fixedEncoding: TileSpatialFixedEncoding; id: string; kind: "tile-spatial-profile"; topology: TileTopology; children: any; comparePath: any; coveredChildren: any; encodeCamera: any; isPathPrefix: any; matrixId: any; matrixLevel: any; normalizedBounds: any; parent: any; path: any; tileBounds: any }>
```

### `webMercatorPlanarTileSpatialProfile`

Kind: `Function`.

Specializes a planar tile profile with WebMercatorQuad addressing and bounds.

```ts
Function webMercatorPlanarTileSpatialProfile
```

```ts
webMercatorPlanarTileSpatialProfile(descriptor: WebMercatorPlanarTileSpatialProfileDescriptor): WebMercatorPlanarTileSpatialProfile
```

### `WebMercatorPlanarTileSpatialProfile`

Kind: `Type Alias`.

```ts
type WebMercatorPlanarTileSpatialProfile = TileSpatialProfile & Readonly<{ addressCodec: WebMercatorQuadAddressCodec }>
```

### `WebMercatorPlanarTileSpatialProfileDescriptor`

Kind: `Type Alias`.

```ts
type WebMercatorPlanarTileSpatialProfileDescriptor = Readonly<{ addressCodec: WebMercatorQuadAddressCodec }>
```

## `packages/geoscratch/src/geo/tile-topology.ts`

### `regularQuadTileTopology`

Kind: `Function`.

Builds regular parent, child, and neighbor relations over a compatible tile matrix set.

```ts
Function regularQuadTileTopology
```

```ts
regularQuadTileTopology(descriptor: RegularQuadTileTopologyDescriptor): TileTopology
```

### `RegularQuadTileTopologyDescriptor`

Kind: `Type Alias`.

```ts
type RegularQuadTileTopologyDescriptor = Readonly<{ id: string; tileMatrixSet: TileMatrixSet }>
```

### `TileTopology`

Kind: `Type Alias`.

```ts
type TileTopology = Readonly<{ id: string; kind: "tile-topology"; roots: readonly TileCoordinate[]; tileMatrixSet: TileMatrixSet; childOrdinal: any; children: any; comparePath: any; isPathPrefix: any; matrixLevel: any; normalizedBounds: any; parent: any; path: any }>
```

## `packages/geoscratch/src/geo/view-tile-demand.ts`

### `ViewDemandProducer`

Kind: `Class`.

Converts view-derived candidates into bounded prioritized tile-demand intent.

```ts
class ViewDemandProducer
```

Members:

- `constructor`: `Constructor constructor`
  - `constructor(descriptor: ViewDemandProducerDescriptor): ViewDemandProducer`
- `id`: `id: string` (readonly)
- `kind`: `kind: "view-demand-producer"` (readonly)
- `maxDemands`: `maxDemands: number` (readonly)
- `produce`: `Method produce`
  - `produce(input: ViewDemandProduction): ViewTileDemandSet`

### `ViewDemandProducerDescriptor`

Kind: `Type Alias`.

```ts
type ViewDemandProducerDescriptor = Readonly<{ id: string; maxDemands: number }>
```

### `ViewDemandProduction`

Kind: `Type Alias`.

```ts
type ViewDemandProduction = Readonly<{ demands: readonly ViewTileDemandDescriptor[]; generation: number; view: GeoViewSnapshot }>
```

### `ViewTileDemand`

Kind: `Type Alias`.

```ts
type ViewTileDemand = Readonly<{ deadlineMs?: number; desiredSampleLevel: number; generation: number; intent: ViewTileDemandIntent; page: VirtualRasterPageIdentity; priority: WorkerTaskPriority; reason: string; source: Readonly<{ frameEpoch: number; kind: "view"; producerId: string; residencySnapshotEpoch: number; viewId: string }>; sourceLevelCeiling: number }>
```

### `ViewTileDemandDescriptor`

Kind: `Type Alias`.

```ts
type ViewTileDemandDescriptor = Readonly<{ deadlineMs?: number; desiredSampleLevel: number; intent: ViewTileDemandIntent; page: VirtualRasterPageIdentity; priority: WorkerTaskPriority; reason: string; sourceLevelCeiling: number }>
```

### `ViewTileDemandIntent`

Kind: `Type Alias`.

```ts
type ViewTileDemandIntent = "coverage" | "refinement" | "prefetch"
```

### `ViewTileDemandSet`

Kind: `Type Alias`.

```ts
type ViewTileDemandSet = Readonly<{ demands: readonly ViewTileDemand[]; generation: number; kind: "view-tile-demand-set" }>
```

### `virtualRasterDemandSetFromViewDemands`

Kind: `Function`.

Lowers one view-demand generation into Virtual Raster request semantics.

```ts
Function virtualRasterDemandSetFromViewDemands
```

```ts
virtualRasterDemandSetFromViewDemands(demandSet: ViewTileDemandSet): VirtualRasterDemandSet
```

## `packages/geoscratch/src/geo/virtual-raster-cache-address.ts`

### `virtualRasterCacheAddress`

Kind: `Function`.

Maps one versioned raster page and coherence contract into generic persistent-cache addresses.

```ts
Function virtualRasterCacheAddress
```

```ts
virtualRasterCacheAddress(descriptor: VirtualRasterCacheAddressDescriptor): VirtualRasterCacheAddress
```

### `VirtualRasterCacheAddress`

Kind: `Type Alias`.

```ts
type VirtualRasterCacheAddress = Readonly<{ invalidationPrefixes: VirtualRasterCacheInvalidationPrefixes; key: PersistentCacheKey; kind: "virtual-raster-cache-address"; metadata: VirtualRasterCacheMetadata }>
```

### `VirtualRasterCacheAddressDescriptor`

Kind: `Type Alias`.

```ts
type VirtualRasterCacheAddressDescriptor = Readonly<{ coherence: VirtualRasterCacheCoherence; decoderVersion: string; matrixId: string; payloadRepresentation: string; plane: string; sampleType: string; schemaVersion: number; sourceId: string; sourceRepresentation: string; tileColumn: number; tileMatrixSetId: string; tileMatrixSetUri: string; tileRow: number }>
```

### `VirtualRasterCacheCoherence`

Kind: `Type Alias`.

```ts
type VirtualRasterCacheCoherence = Readonly<{ contentVersion: string; mode: "immutable" }> | Readonly<{ mode: "revisioned"; revision: string; validator?: string }> | Readonly<{ baseRevision: string; mode: "editable" }>
```

### `VirtualRasterCacheInvalidationPrefixes`

Kind: `Type Alias`.

```ts
type VirtualRasterCacheInvalidationPrefixes = Readonly<{ matrix: string; plane: string; source: string; tileMatrixSet: string }>
```

### `VirtualRasterCacheMetadata`

Kind: `Type Alias`.

```ts
type VirtualRasterCacheMetadata = Readonly<{ coherence: VirtualRasterCacheCoherence; decoderVersion: string; domain: "geo.virtual-raster"; matrixId: string; payloadRepresentation: string; plane: string; sampleType: string; schemaVersion: number; sourceId: string; sourceRepresentation: string; tileColumn: number; tileMatrixSetId: string; tileMatrixSetUri: string; tileRow: number }>
```

## `packages/geoscratch/src/geo/virtual-raster-demand.ts`

### `VirtualRasterDemandGenerationFacts`

Kind: `Type Alias`.

```ts
type VirtualRasterDemandGenerationFacts = Readonly<{ failedCount: number; generation: number; residentCount: number; stagedCount: number; staleCount: number }>
```

### `VirtualRasterDemandHistoryEntry`

Kind: `Type Alias`.

```ts
type VirtualRasterDemandHistoryEntry = Readonly<{ detail?: string; generation: number; kind: "requested" | "retained" | "reprioritized" | "cancelled" | "staged" | "resident" | "stale" | "failed" | "dropped" | "disposed"; pageKey: string; sequence: number }>
```

### `VirtualRasterDemandReconciliation`

Kind: `Type Alias`.

```ts
type VirtualRasterDemandReconciliation = Readonly<{ cancelledCount: number; droppedCount: number; generation: number; requestedCount: number; retainedCount: number; settled: Promise<VirtualRasterDemandGenerationFacts> }>
```

### `virtualRasterDemandSet`

Kind: `Function`.

Validates and freezes one uniquely keyed generation of prioritized raster-page intent.

```ts
Function virtualRasterDemandSet
```

```ts
virtualRasterDemandSet(descriptor: VirtualRasterDemandSetDescriptor): VirtualRasterDemandSet
```

### `VirtualRasterDemandSet`

Kind: `Type Alias`.

```ts
type VirtualRasterDemandSet = Readonly<{ demands: readonly VirtualRasterPageDemand[]; generation: number; kind: "virtual-raster-demand-set" }>
```

### `VirtualRasterDemandSetDescriptor`

Kind: `Type Alias`.

```ts
type VirtualRasterDemandSetDescriptor = Readonly<{ demands: readonly VirtualRasterPageDemand[]; generation: number }>
```

### `VirtualRasterDemandUsage`

Kind: `Type Alias`.

```ts
type VirtualRasterDemandUsage = "required" | "prefetch"
```

### `VirtualRasterPageDemand`

Kind: `Type Alias`.

```ts
type VirtualRasterPageDemand = Readonly<{ deadlineMs?: number; generation: number; page: VirtualRasterPageIdentity; priority: WorkerTaskPriority; reason: string; usage: VirtualRasterDemandUsage }>
```

### `VirtualRasterRequestExecution`

Kind: `Type Alias`.

```ts
type VirtualRasterRequestExecution = Readonly<{ result: Promise<VirtualRasterPageTransfer>; accept: any; cancel: any; classifyFailure?: any; discard: any; inspect?: any; reprioritize: any }>
```

### `VirtualRasterRequestExecutionFacts`

Kind: `Type Alias`.

```ts
type VirtualRasterRequestExecutionFacts = Readonly<{ phase?: "cache" | "network" | "decode"; state: WorkerTaskState }>
```

### `VirtualRasterRequestExecutor`

Kind: `Type Alias`.

```ts
type VirtualRasterRequestExecutor = Readonly<{ request: any }>
```

### `VirtualRasterRequestFailureClassification`

Kind: `Type Alias`.

```ts
type VirtualRasterRequestFailureClassification = Readonly<{ code?: string; detail?: string; disposition: "retryable" | "terminal" }>
```

### `VirtualRasterRequestScheduler`

Kind: `Class`.

Reconciles generation-tagged page demand with bounded asynchronous request execution.

```ts
class VirtualRasterRequestScheduler
```

Members:

- `constructor`: `Constructor constructor`
  - `constructor(descriptor: VirtualRasterRequestSchedulerDescriptor): VirtualRasterRequestScheduler`
- `dispose`: `Method dispose`
  - `dispose(): Promise<void>`
- `executor`: `executor: VirtualRasterRequestExecutor` (readonly)
- `inspect`: `Method inspect`
  - `inspect(): VirtualRasterRequestSchedulerFacts`
- `maxHistory`: `maxHistory: number` (readonly)
- `maxRequests`: `maxRequests: number` (readonly)
- `publish`: `Method publish`
  - `publish(): VirtualRasterPublication`
- `reconcile`: `Method reconcile`
  - `reconcile(demandSet: VirtualRasterDemandSet): VirtualRasterDemandReconciliation`
- `residency`: `residency: VirtualRasterResidency` (readonly)

### `VirtualRasterRequestSchedulerDescriptor`

Kind: `Type Alias`.

```ts
type VirtualRasterRequestSchedulerDescriptor = Readonly<{ executor: VirtualRasterRequestExecutor; maxHistory?: number; maxRequests: number; residency: VirtualRasterResidency }>
```

### `VirtualRasterRequestSchedulerFacts`

Kind: `Type Alias`.

```ts
type VirtualRasterRequestSchedulerFacts = Readonly<{ activeDecodeCount: number; activeNetworkCount: number; activeRequestCount: number; cancellationCount: number; completedRequestCount: number; degradationCount: number; demandedPageCount: number; disposed: boolean; failedRequestCount: number; generation: number; history: readonly VirtualRasterDemandHistoryEntry[]; maxRequests: number; queuedRequestCount: number; reprioritizationCount: number; staleResultCount: number }>
```

## `packages/geoscratch/src/geo/virtual-raster-gpu.ts`

### `createVirtualRasterGpuState`

Kind: `Function`.

Asynchronously allocates GPU residency state for one address space and physical plane.

```ts
Function createVirtualRasterGpuState
```

```ts
createVirtualRasterGpuState(runtime: GPURuntime, descriptor: VirtualRasterGpuStateDescriptor): Promise<VirtualRasterGpuState>
```

### `VirtualRasterGpuFacts`

Kind: `Type Alias`.

```ts
type VirtualRasterGpuFacts = Readonly<{ acknowledgementSerial: number; atlasColumns: number; atlasHeight: number; atlasRows: number; atlasWidth: number; maxPhysicalPages: number; pageTableBytes: number; pageTableEntryCount: number; slotTableBytes: number; snapshotEpoch: number; stagedSnapshotEpoch?: number }>
```

### `VirtualRasterGpuState`

Kind: `Class`.

Owns the finite atlas and mapping buffers that publish coherent Virtual Raster snapshots.

```ts
class VirtualRasterGpuState
```

Members:

- `abandon`: `Method abandon`
  - `abandon(publication: VirtualRasterPublication): Promise<void>`
- `acknowledge`: `Method acknowledge`
  - `acknowledge(publication: VirtualRasterPublication, submitted: SubmittedWork): Promise<void>`
- `acknowledges`: `Method acknowledges`
  - `acknowledges(snapshot: VirtualRasterSnapshot): boolean`
- `addressSpace`: `addressSpace: VirtualRasterAddressSpace` (readonly)
- `atlas`: `atlas: TextureResource` (readonly)
- `atlasColumns`: `atlasColumns: number` (readonly)
- `atlasHeight`: `atlasHeight: number` (readonly)
- `atlasRows`: `atlasRows: number` (readonly)
- `atlasView`: `atlasView: TextureViewSpec` (readonly)
- `atlasWidth`: `atlasWidth: number` (readonly)
- `create`: `Method create` (static)
  - `create(runtime: GPURuntime, descriptor: VirtualRasterGpuStateDescriptor): Promise<VirtualRasterGpuState>`
- `dispose`: `Method dispose`
  - `dispose(): void`
- `encode`: `Method encode`
  - `encode(builder: SubmissionBuilder, update: VirtualRasterGpuUpdate): SubmissionBuilder`
- `facts`: `Method facts`
  - `facts(): VirtualRasterGpuFacts`
- `maxPhysicalPages`: `maxPhysicalPages: number` (readonly)
- `pageTable`: `pageTable: BufferResource` (readonly)
- `plane`: `plane: VirtualRasterPlane` (readonly)
- `runtime`: `runtime: GPURuntime` (readonly)
- `slotTable`: `slotTable: BufferResource` (readonly)
- `stage`: `Method stage`
  - `stage(publication: VirtualRasterPublication): VirtualRasterGpuUpdate`

### `VirtualRasterGpuStateDescriptor`

Kind: `Type Alias`.

```ts
type VirtualRasterGpuStateDescriptor = Readonly<{ addressSpace: VirtualRasterAddressSpace; maxPhysicalPages: number; plane: VirtualRasterPlane }>
```

### `VirtualRasterGpuUpdate`

Kind: `Type Alias`.

```ts
type VirtualRasterGpuUpdate = Readonly<{ atlasUploads: readonly TextureUploadCommand[]; commands: readonly (TextureUploadCommand | UploadCommand)[]; pageTableUpload?: UploadCommand; slotTableUpload?: UploadCommand; snapshotEpoch: number }>
```

## `packages/geoscratch/src/geo/virtual-raster-residency.ts`

### `VirtualRasterFailureOptions`

Kind: `Type Alias`.

```ts
type VirtualRasterFailureOptions = Readonly<{ detail?: string; generation: number }>
```

### `VirtualRasterHistoryEntry`

Kind: `Type Alias`.

```ts
type VirtualRasterHistoryEntry = Readonly<{ detail?: string; kind: "staged" | "resident" | "fallback" | "eviction" | "failed" | "stale" | "pin" | "unpin" | "publish" | "acknowledge" | "abandon" | "dispose"; pageKey: string; sequence: number }>
```

### `VirtualRasterPageAvailability`

Kind: `Type Alias`.

```ts
type VirtualRasterPageAvailability = "staged" | "resident" | "failed" | "missing"
```

### `VirtualRasterPublication`

Kind: `Class`.

Represents a staged snapshot that becomes visible only after explicit commit.

```ts
class VirtualRasterPublication
```

Members:

- `abandon`: `Method abandon`
  - `abandon(): Promise<void>`
- `acknowledge`: `Method acknowledge`
  - `acknowledge(): Promise<void>`
- `inspect`: `Method inspect`
  - `inspect(): VirtualRasterPublicationFacts`
- `kind`: `kind: "virtual-raster-publication"` (readonly)
- `snapshot`: `snapshot: VirtualRasterSnapshot` (readonly)
- `uploads`: `uploads: readonly Readonly<{ byteLength: number; channels: number; contentEpoch: number; contentVersion: string; generation: number; height: number; page: VirtualRasterPageIdentity; physicalSlot: number; width: number }>[]` (readonly)

### `VirtualRasterPublicationFacts`

Kind: `Type Alias`.

```ts
type VirtualRasterPublicationFacts = Readonly<{ epoch: number; stagingBytes: number; state: VirtualRasterPublicationState; uploadPageCount: number }>
```

### `VirtualRasterPublicationState`

Kind: `Type Alias`.

```ts
type VirtualRasterPublicationState = "pending" | "settling" | "acknowledged" | "abandoned"
```

### `VirtualRasterResidency`

Kind: `Class`.

Owns bounded CPU staging, slot allocation, coherent snapshots, and residency history.

```ts
class VirtualRasterResidency
```

Members:

- `addressSpace`: `addressSpace: VirtualRasterAddressSpace` (readonly)
- `availability`: `Method availability`
  - `availability(page: VirtualRasterPageIdentity): VirtualRasterPageAvailability`
- `constructor`: `Constructor constructor`
  - `constructor(descriptor: VirtualRasterResidencyDescriptor): VirtualRasterResidency`
- `createLease`: `Method createLease`
  - `createLease(descriptor: VirtualRasterResidencyLeaseDescriptor): VirtualRasterResidencyLease`
- `currentSnapshot`: `Accessor currentSnapshot`
  - `currentSnapshot(): VirtualRasterSnapshot`
- `dispose`: `Method dispose`
  - `dispose(): void`
- `fail`: `Method fail`
  - `fail(page: VirtualRasterPageIdentity, options: VirtualRasterFailureOptions): VirtualRasterStageOutcome`
- `inspect`: `Method inspect`
  - `inspect(): VirtualRasterResidencyFacts`
- `markUsed`: `Method markUsed`
  - `markUsed(page: VirtualRasterPageIdentity): void`
- `maxHistory`: `maxHistory: number` (readonly)
- `maxPhysicalPages`: `maxPhysicalPages: number` (readonly)
- `maxStagingBytes`: `maxStagingBytes: number` (readonly)
- `pin`: `Method pin`
  - `pin(page: VirtualRasterPageIdentity): void`
- `plane`: `plane: VirtualRasterPlane` (readonly)
- `publish`: `Method publish`
  - `publish(): VirtualRasterPublication`
- `reconcileGeneration`: `Method reconcileGeneration`
  - `reconcileGeneration(generation: number, requiredPages: readonly Readonly<{ addressSpaceId: string; coordinates: readonly number[]; dimensions: CoordinateDimension; key: string; kind: "virtual-raster-page"; level: number; tile?: Readonly<{ key: string; kind: "tile-coordinate"; matrixId: string; tileCol: number; tileMatrixSetId: string; tileRow: number }> }>[] = []): void`
- `stage`: `Method stage`
  - `stage(payload: OwnedVirtualRasterPagePayload, options: VirtualRasterStageOptions): VirtualRasterStageOutcome`
- `unpin`: `Method unpin`
  - `unpin(page: VirtualRasterPageIdentity): void`

### `VirtualRasterResidencyDescriptor`

Kind: `Type Alias`.

```ts
type VirtualRasterResidencyDescriptor = Readonly<{ addressSpace: VirtualRasterAddressSpace; maxHistory?: number; maxPhysicalPages: number; maxStagingBytes: number; plane: VirtualRasterPlane }>
```

### `VirtualRasterResidencyFacts`

Kind: `Type Alias`.

```ts
type VirtualRasterResidencyFacts = Readonly<{ demandGeneration: number; disposed: boolean; evictionCount: number; failedCount: number; fallbackCount: number; history: readonly VirtualRasterHistoryEntry[]; maxPhysicalPages: number; maxStagingBytes: number; pinnedCount: number; residentCount: number; residentGpuBytes: number; snapshotEpoch: number; stagedCount: number; stagingBytes: number; staleResponseCount: number }>
```

### `VirtualRasterResidencyLease`

Kind: `Class`.

Pins referenced physical slots until all submissions that may sample them are retired.

```ts
class VirtualRasterResidencyLease
```

Members:

- `dispose`: `Method dispose`
  - `dispose(): void`
- `facts`: `Method facts`
  - `facts(): VirtualRasterResidencyLeaseFacts`
- `release`: `Method release`
  - `release(page: VirtualRasterPageIdentity, generation: number): boolean`
- `retain`: `Method retain`
  - `retain(page: VirtualRasterPageIdentity, generation: number): boolean`

### `VirtualRasterResidencyLeaseDescriptor`

Kind: `Type Alias`.

```ts
type VirtualRasterResidencyLeaseDescriptor = Readonly<{ id: string; maxHistory?: number; maximumPages: number }>
```

### `VirtualRasterResidencyLeaseFacts`

Kind: `Type Alias`.

```ts
type VirtualRasterResidencyLeaseFacts = Readonly<{ disposed: boolean; history: readonly VirtualRasterResidencyLeaseHistoryEntry[]; id: string; maximumPages: number; retainedPageCount: number; retainedPages: readonly VirtualRasterResidencyLeasePageFacts[] }>
```

### `VirtualRasterResidencyLeaseHistoryEntry`

Kind: `Type Alias`.

```ts
type VirtualRasterResidencyLeaseHistoryEntry = Readonly<{ generation: number; kind: "retain" | "release" | "stale-retain" | "stale-release" | "invalidate" | "dispose"; pageKey: string; sequence: number }>
```

### `VirtualRasterResidencyLeasePageFacts`

Kind: `Type Alias`.

```ts
type VirtualRasterResidencyLeasePageFacts = Readonly<{ generation: number; pageKey: string }>
```

### `VirtualRasterStageOptions`

Kind: `Type Alias`.

```ts
type VirtualRasterStageOptions = Readonly<{ generation: number }>
```

### `VirtualRasterStageOutcome`

Kind: `Type Alias`.

```ts
type VirtualRasterStageOutcome = Readonly<{ detail?: string; generation: number; page: VirtualRasterPageIdentity; status: VirtualRasterStageStatus }>
```

### `VirtualRasterStageStatus`

Kind: `Type Alias`.

```ts
type VirtualRasterStageStatus = "staged" | "resident" | "stale" | "failed" | "disposed"
```

### `VirtualRasterUploadPage`

Kind: `Type Alias`.

```ts
type VirtualRasterUploadPage = Readonly<{ byteLength: number; channels: number; contentEpoch: number; contentVersion: string; generation: number; height: number; page: VirtualRasterPageIdentity; physicalSlot: number; width: number }>
```

## `packages/geoscratch/src/geo/virtual-raster-runtime.ts`

### `createVirtualRasterDemandController`

Kind: `Function`.

Coordinates safety cover, view demand, feedback acknowledgement, and publication retention.

```ts
Function createVirtualRasterDemandController
```

```ts
createVirtualRasterDemandController<Model extends Readonly<{ addressSpace: VirtualRasterAddressSpace; field: GeoField; id: string; plane: VirtualRasterPlane; representation: TiledFieldRepresentation; safetyCoverPages: readonly Readonly<{ addressSpaceId: string; coordinates: readonly number[]; dimensions: CoordinateDimension; key: string; kind: "virtual-raster-page"; level: number; tile?: Readonly<{ key: string; kind: "tile-coordinate"; matrixId: string; tileCol: number; tileMatrixSetId: string; tileRow: number }> }>[]; spatialProfile: TileSpatialProfile }>>(__namedParameters: VirtualRasterDemandControllerDescriptor<Model>): VirtualRasterDemandController
```

### `createVirtualRasterRuntime`

Kind: `Function`.

Assembles request scheduling, CPU residency, GPU publication, demand, and explicit executor authority.

```ts
Function createVirtualRasterRuntime
```

```ts
createVirtualRasterRuntime<Model extends Readonly<{ addressSpace: VirtualRasterAddressSpace; field: GeoField; id: string; plane: VirtualRasterPlane; representation: TiledFieldRepresentation; safetyCoverPages: readonly Readonly<{ addressSpaceId: string; coordinates: readonly number[]; dimensions: CoordinateDimension; key: string; kind: "virtual-raster-page"; level: number; tile?: Readonly<{ key: string; kind: "tile-coordinate"; matrixId: string; tileCol: number; tileMatrixSetId: string; tileRow: number }> }>[]; spatialProfile: TileSpatialProfile }>>(__namedParameters: VirtualRasterRuntimeDescriptor<Model>): Promise<VirtualRasterRuntime<Model>>
```

### `VirtualRasterDemandController`

Kind: `Type Alias`.

```ts
type VirtualRasterDemandController = Readonly<{ abandonPublication: any; acknowledgePublication: any; dispose: any; facts: any; initialize: any; reconcileViewDemands: any }>
```

### `VirtualRasterDemandControllerDescriptor`

Kind: `Type Alias`.

```ts
type VirtualRasterDemandControllerDescriptor<Model extends VirtualRasterRuntimeModel = VirtualRasterRuntimeModel> = Readonly<{ maxHistory: number; maxPhysicalPages: number; model: Model; residency: VirtualRasterResidency; scheduler: VirtualRasterRequestScheduler; viewDemandProducer: ViewDemandProducer }>
```

### `VirtualRasterDemandControllerFacts`

Kind: `Type Alias`.

```ts
type VirtualRasterDemandControllerFacts = Readonly<{ acknowledgedSnapshotEpoch: number; activeDemandCount: number; disposed: boolean; generation: number; lastDecisionFrameEpoch: number; safetyDemandCount: number; viewDemandCapacity: number }>
```

### `VirtualRasterExecutorBinding`

Kind: `Type Alias`.

Declares whether one Virtual Raster runtime borrows or owns its request executor.

```ts
type VirtualRasterExecutorBinding = Readonly<{ executor: VirtualRasterRequestExecutor; ownership: "borrowed" }> | Readonly<{ executor: VirtualRasterRequestExecutor & Readonly<{ dispose: any }>; ownership: "owned" }>
```

### `VirtualRasterFeedbackReconciliation`

Kind: `Type Alias`.

```ts
type VirtualRasterFeedbackReconciliation = Readonly<{ generation: number; requestedCount: number; retainedCount: number; retiredCount: number; settlement: VirtualRasterDemandReconciliation["settled"] }>
```

### `VirtualRasterRuntime`

Kind: `Type Alias`.

```ts
type VirtualRasterRuntime<Model extends VirtualRasterRuntimeModel = VirtualRasterRuntimeModel> = Omit<Model, "kind"> & Readonly<{ gpu: VirtualRasterGpuState; kind: "virtual-raster-runtime"; model: Model; residency: VirtualRasterResidency; scheduler: VirtualRasterRequestScheduler; viewDemandProducer: ViewDemandProducer; acknowledge: any; dispose: any; initialize: any; inspect: any; publish: any; reconcileViewDemands: any; stopDemand: any }>
```

### `VirtualRasterRuntimeDescriptor`

Kind: `Type Alias`.

```ts
type VirtualRasterRuntimeDescriptor<Model extends VirtualRasterRuntimeModel = VirtualRasterRuntimeModel> = Readonly<{ executor: VirtualRasterExecutorBinding; maxHistory: number; maxPhysicalPages: number; maxRequests: number; maxStagingBytes: number; model: Model; runtime: GPURuntime; viewDemandProducerId?: string }>
```

### `VirtualRasterRuntimeFacts`

Kind: `Type Alias`.

```ts
type VirtualRasterRuntimeFacts = Readonly<{ demand: VirtualRasterDemandControllerFacts; demandStopped: boolean; disposed: boolean; executorOwnership: VirtualRasterExecutorBinding["ownership"]; gpu: ReturnType<VirtualRasterGpuState["facts"]>; id: string; kind: "virtual-raster-runtime"; residency: ReturnType<VirtualRasterResidency["inspect"]>; scheduler: ReturnType<VirtualRasterRequestScheduler["inspect"]> }>
```

### `VirtualRasterRuntimeModel`

Kind: `Type Alias`.

```ts
type VirtualRasterRuntimeModel = Readonly<{ addressSpace: VirtualRasterAddressSpace; field: GeoField; id: string; plane: VirtualRasterPlane; representation: TiledFieldRepresentation; safetyCoverPages: readonly VirtualRasterPageIdentity[]; spatialProfile: TileSpatialProfile }>
```

### `VirtualRasterRuntimePublication`

Kind: `Type Alias`.

```ts
type VirtualRasterRuntimePublication = Readonly<{ changed: boolean; publication: VirtualRasterPublication; snapshotEpoch: number; update: VirtualRasterGpuUpdate }>
```

## `packages/geoscratch/src/geo/virtual-raster-transfer.ts`

### `adoptVirtualRasterPageTransfer`

Kind: `Function`.

Adopts one received transfer as a live locally owned raster-page payload.

```ts
Function adoptVirtualRasterPageTransfer
```

```ts
adoptVirtualRasterPageTransfer(transfer: VirtualRasterPageTransfer): OwnedVirtualRasterPagePayload
```

### `discardOwnedVirtualRasterPagePayload`

Kind: `Function`.

Releases an unclaimed owned payload and detaches its backing buffer.

```ts
Function discardOwnedVirtualRasterPagePayload
```

```ts
discardOwnedVirtualRasterPagePayload(payload: OwnedVirtualRasterPagePayload): void
```

### `discardVirtualRasterPageTransfer`

Kind: `Function`.

Discards an unadopted transfer by detaching its live backing buffer.

```ts
Function discardVirtualRasterPageTransfer
```

```ts
discardVirtualRasterPageTransfer(transfer: VirtualRasterPageTransfer): void
```

### `ownedVirtualRasterPagePayload`

Kind: `Function`.

Wraps decoded page bytes in single-owner lifecycle tracking without copying them.

```ts
Function ownedVirtualRasterPagePayload
```

```ts
ownedVirtualRasterPagePayload(descriptor: VirtualRasterPagePayloadDescriptor): OwnedVirtualRasterPagePayload
```

### `OwnedVirtualRasterPagePayload`

Kind: `Type Alias`.

```ts
type OwnedVirtualRasterPagePayload = Readonly<{ channels: number; contentVersion: string; data: VirtualRasterPageData; height: number; kind: "owned-virtual-raster-page-payload"; page: VirtualRasterPageIdentity; width: number }>
```

### `PreparedVirtualRasterPageTransfer`

Kind: `Type Alias`.

```ts
type PreparedVirtualRasterPageTransfer = Readonly<{ transferables: readonly [ArrayBuffer]; value: VirtualRasterPageTransfer }>
```

### `prepareVirtualRasterPageTransfer`

Kind: `Function`.

Prepares decoded page bytes and their backing buffer for one ownership-moving postMessage.

```ts
Function prepareVirtualRasterPageTransfer
```

```ts
prepareVirtualRasterPageTransfer(descriptor: VirtualRasterPagePayloadDescriptor): PreparedVirtualRasterPageTransfer
```

### `VirtualRasterPageData`

Kind: `Type Alias`.

```ts
type VirtualRasterPageData = Uint8Array<ArrayBuffer> | Float32Array<ArrayBuffer>
```

### `VirtualRasterPagePayloadDescriptor`

Kind: `Type Alias`.

```ts
type VirtualRasterPagePayloadDescriptor = Readonly<{ channels: number; contentVersion: string; data: VirtualRasterPageData; height: number; page: VirtualRasterPageIdentity; width: number }>
```

### `VirtualRasterPageTransfer`

Kind: `Type Alias`.

```ts
type VirtualRasterPageTransfer = Readonly<{ buffer: ArrayBuffer; channels: number; contentVersion: string; dataType: "uint8" | "float32"; height: number; kind: "virtual-raster-page-transfer"; page: VirtualRasterPageIdentity; width: number }>
```

## `packages/geoscratch/src/geo/virtual-raster-worker-executor.ts`

### `createVirtualRasterWorkerExecutor`

Kind: `Function`.

Adapts retained Scratch Worker contexts and phase budgets into raster page requests.

```ts
Function createVirtualRasterWorkerExecutor
```

```ts
createVirtualRasterWorkerExecutor<Candidate extends Readonly<{ candidateId: string; page: VirtualRasterPageIdentity }>, Init, WorkerFacts>(descriptor: VirtualRasterWorkerExecutorDescriptor<Candidate, Init, WorkerFacts>): Promise<VirtualRasterWorkerExecutor<WorkerFacts>>
```

### `VirtualRasterWorkerCandidate`

Kind: `Type Alias`.

```ts
type VirtualRasterWorkerCandidate = Readonly<{ candidateId: string; page: VirtualRasterPageIdentity }>
```

### `VirtualRasterWorkerContextDescriptor`

Kind: `Type Alias`.

```ts
type VirtualRasterWorkerContextDescriptor<Init> = Readonly<{ init: Init; key: string; restore?: unknown }>
```

### `VirtualRasterWorkerExecutor`

Kind: `Type Alias`.

```ts
type VirtualRasterWorkerExecutor<WorkerFacts> = VirtualRasterRequestExecutor & Readonly<{ dispose: any; inspect: any; refreshFacts: any }>
```

### `VirtualRasterWorkerExecutorDescriptor`

Kind: `Type Alias`.

```ts
type VirtualRasterWorkerExecutorDescriptor<Candidate extends VirtualRasterWorkerCandidate, Init, WorkerFacts> = Readonly<{ disposeGraceMs?: number; id: string; idleTimeoutMs?: number; maxRequests: number; module: WorkerModuleReference; phaseLimits: Readonly<Record<VirtualRasterWorkerPhase, number>>; system: WorkerContextPoolSystem; workerCount: number; candidate: any; classifyFailure?: any; context: any; staleKey?: any }>
```

### `VirtualRasterWorkerExecutorFacts`

Kind: `Type Alias`.

```ts
type VirtualRasterWorkerExecutorFacts<WorkerFacts> = Readonly<{ contextPool: WorkerContextPoolFacts; disposed: boolean; group: WorkerGroupFacts; id: string; kind: "virtual-raster-worker-executor"; phaseBudget: TaskPhaseBudgetFacts<VirtualRasterWorkerPhase>; system: WorkerSystemFacts; workerFactsObservation: "live" | "last-observed-before-disposal"; workers: readonly WorkerFacts[] }>
```

### `VirtualRasterWorkerLookupResult`

Kind: `Type Alias`.

```ts
type VirtualRasterWorkerLookupResult = Readonly<{ candidateId?: string; status: "hit" | "miss" }>
```

### `VirtualRasterWorkerModuleProtocol`

Kind: `Type Alias`.

```ts
type VirtualRasterWorkerModuleProtocol<Candidate extends VirtualRasterWorkerCandidate, Init, WorkerFacts, FetchResult = unknown> = WorkerModuleProtocol<WorkerNoOperations, WorkerContextProtocol<Init, VirtualRasterWorkerProtocol<Candidate, WorkerFacts, FetchResult>>>
```

### `VirtualRasterWorkerPhase`

Kind: `Type Alias`.

```ts
type VirtualRasterWorkerPhase = "network" | "decode"
```

### `VirtualRasterWorkerProtocol`

Kind: `Type Alias`.

```ts
type VirtualRasterWorkerProtocol<Candidate extends VirtualRasterWorkerCandidate, WorkerFacts, FetchResult = unknown> = Readonly<{ accept: WorkerOperationProtocol<Readonly<{ candidateId: string }>, WorkerFacts>; decode: WorkerOperationProtocol<Readonly<{ candidateId: string }>, VirtualRasterPageTransfer>; discard: WorkerOperationProtocol<Readonly<{ candidateId: string }>, WorkerFacts>; facts: WorkerOperationProtocol<null, WorkerFacts>; fetch: WorkerOperationProtocol<Candidate, FetchResult>; lookup: WorkerOperationProtocol<Candidate, VirtualRasterWorkerLookupResult>; transfer: WorkerOperationProtocol<Readonly<{ candidateId: string }>, VirtualRasterPageTransfer> }>
```

## `packages/geoscratch/src/geo/virtual-raster.ts`

### `virtualRasterAccessor`

Kind: `Function`.

Creates a logical CPU and WGSL accessor over one address space and plane.

```ts
Function virtualRasterAccessor
```

```ts
virtualRasterAccessor(descriptor: VirtualRasterAccessorDescriptor): VirtualRasterAccessor
```

### `VirtualRasterAccessor`

Kind: `Class`.

Resolves CPU samples and generates shader accessors without exposing atlas placement to callers.

```ts
class VirtualRasterAccessor
```

Members:

- `addressSpace`: `addressSpace: VirtualRasterAddressSpace` (readonly)
- `constructor`: `Constructor constructor`
  - `constructor(descriptor: VirtualRasterAccessorDescriptor): VirtualRasterAccessor`
- `plane`: `plane: VirtualRasterPlane` (readonly)
- `sample`: `Method sample`
  - `sample(snapshot: VirtualRasterSnapshot, descriptor: VirtualRasterSampleDescriptor, cpuPages: VirtualRasterCpuPageProvider): VirtualRasterSample`
- `wgslModule`: `Method wgslModule`
  - `wgslModule(options: VirtualRasterAccessorWgslOptions): string`

### `VirtualRasterAccessorDescriptor`

Kind: `Type Alias`.

```ts
type VirtualRasterAccessorDescriptor = Readonly<{ addressSpace: VirtualRasterAddressSpace; plane: VirtualRasterPlane }>
```

### `VirtualRasterAccessorWgslOptions`

Kind: `Type Alias`.

```ts
type VirtualRasterAccessorWgslOptions = Readonly<{ atlasBinding: number; group: number; namespace?: string; pageTableBinding: number }>
```

### `virtualRasterAddressSpace`

Kind: `Function`.

Creates a regular finite-dimensional Virtual Raster address space.

```ts
Function virtualRasterAddressSpace
```

```ts
virtualRasterAddressSpace(descriptor: VirtualRasterAddressSpaceDescriptor): VirtualRasterAddressSpace
```

### `VirtualRasterAddressSpace`

Kind: `Class`.

Maps finite logical raster coordinates or tile coverage into compact page identities.

```ts
class VirtualRasterAddressSpace
```

Members:

- `assertPage`: `Method assertPage`
  - `assertPage(page: VirtualRasterPageIdentity): void`
- `constructor`: `Constructor constructor`
  - `constructor(descriptor: Readonly<{ dimensions: CoordinateDimension; extent: readonly number[]; id: string; levelCount: number; pageSize: readonly number[] }> | Readonly<{ coverage: TileMatrixCoverage; id: string }>): VirtualRasterAddressSpace`
- `dimensions`: `dimensions: CoordinateDimension` (readonly)
- `extent`: `extent: readonly number[]` (readonly)
- `id`: `id: string` (readonly)
- `kind`: `kind: "virtual-raster-address-space"` (readonly)
- `levelCount`: `levelCount: number` (readonly)
- `levelExtent`: `Method levelExtent`
  - `levelExtent(level: number): readonly number[]`
- `levelForMatrix`: `Method levelForMatrix`
  - `levelForMatrix(matrixId: string): number`
- `levelOffset`: `Method levelOffset`
  - `levelOffset(level: number): number`
- `matrixId`: `Method matrixId`
  - `matrixId(level: number): string`
- `page`: `Method page`
  - `page(descriptor: VirtualRasterPageDescriptor): VirtualRasterPageIdentity`
- `pageFromTile`: `Method pageFromTile`
  - `pageFromTile(descriptor: TileCoordinateDescriptor): VirtualRasterPageIdentity`
- `pageGrid`: `Method pageGrid`
  - `pageGrid(level: number): readonly number[]`
- `pages`: `Method pages`
  - `pages(): readonly Readonly<{ addressSpaceId: string; coordinates: readonly number[]; dimensions: CoordinateDimension; key: string; kind: "virtual-raster-page"; level: number; tile?: Readonly<{ key: string; kind: "tile-coordinate"; matrixId: string; tileCol: number; tileMatrixSetId: string; tileRow: number }> }>[]`
- `pageSize`: `pageSize: readonly number[]` (readonly)
- `pageTableEntryCount`: `pageTableEntryCount: number` (readonly)
- `parent`: `Method parent`
  - `parent(page: VirtualRasterPageIdentity): Readonly<{ addressSpaceId: string; coordinates: readonly number[]; dimensions: CoordinateDimension; key: string; kind: "virtual-raster-page"; level: number; tile?: Readonly<{ key: string; kind: "tile-coordinate"; matrixId: string; tileCol: number; tileMatrixSetId: string; tileRow: number }> }> | undefined`
- `rootPage`: `Method rootPage`
  - `rootPage(): VirtualRasterPageIdentity`
- `tableIndex`: `Method tableIndex`
  - `tableIndex(page: VirtualRasterPageIdentity): number`
- `tileCoverage`: `tileCoverage: TileMatrixCoverage` (optional, readonly)

### `VirtualRasterAddressSpaceDescriptor`

Kind: `Type Alias`.

```ts
type VirtualRasterAddressSpaceDescriptor = Readonly<{ dimensions: CoordinateDimension; extent: readonly number[]; id: string; levelCount: number; pageSize: readonly number[] }>
```

### `VirtualRasterCpuPage`

Kind: `Type Alias`.

```ts
type VirtualRasterCpuPage = Readonly<{ channels: number; data: VirtualRasterPageData; height: number; page: VirtualRasterPageIdentity; width: number }>
```

### `VirtualRasterCpuPageProvider`

Kind: `Type Alias`.

```ts
type VirtualRasterCpuPageProvider = Readonly<{ get: any }>
```

### `VirtualRasterFieldKind`

Kind: `Type Alias`.

```ts
type VirtualRasterFieldKind = "scalar" | "color" | "categorical" | "mask" | "vector" | "multiband"
```

### `VirtualRasterFilter`

Kind: `Type Alias`.

```ts
type VirtualRasterFilter = "nearest" | "bilinear"
```

### `VirtualRasterOuterBoundary`

Kind: `Type Alias`.

```ts
type VirtualRasterOuterBoundary = "clamp" | "no-data"
```

### `VirtualRasterPageDescriptor`

Kind: `Type Alias`.

```ts
type VirtualRasterPageDescriptor = Readonly<{ level: number; x: number; y?: number; z?: number }>
```

### `VirtualRasterPageIdentity`

Kind: `Type Alias`.

```ts
type VirtualRasterPageIdentity = Readonly<{ addressSpaceId: string; coordinates: readonly number[]; dimensions: CoordinateDimension; key: string; kind: "virtual-raster-page"; level: number; tile?: TileCoordinate }>
```

### `VirtualRasterPageTableEntry`

Kind: `Type Alias`.

```ts
type VirtualRasterPageTableEntry = Readonly<{ contentEpoch?: number; generation?: number; physicalSlot?: number; requestedLevel: number; requestedPage: VirtualRasterPageIdentity; resolvedLevel?: number; resolvedPage?: VirtualRasterPageIdentity; status: VirtualRasterSnapshotResolveStatus }>
```

### `VirtualRasterPhysicalPage`

Kind: `Type Alias`.

```ts
type VirtualRasterPhysicalPage = Readonly<{ byteLength: number; channels: number; contentEpoch: number; contentVersion: string; generation: number; height: number; page: VirtualRasterPageIdentity; physicalSlot: number; width: number }>
```

### `virtualRasterPlane`

Kind: `Function`.

Defines physical channel, sample, format, and missing-data semantics for one raster plane.

```ts
Function virtualRasterPlane
```

```ts
virtualRasterPlane(descriptor: VirtualRasterPlaneDescriptor): VirtualRasterPlane
```

### `VirtualRasterPlane`

Kind: `Type Alias`.

```ts
type VirtualRasterPlane = Readonly<{ addressSpace: VirtualRasterAddressSpace; auxiliaryAxes: readonly Readonly<{ name: string; value: string | number }>[]; channels: 1 | 2 | 3 | 4; fieldKind: VirtualRasterFieldKind; gpuFormat: GPUTextureFormat; id: string; kind: "virtual-raster-plane"; noData?: number; offset: readonly number[]; sampleType: VirtualRasterSampleType; scale: readonly number[] }>
```

### `VirtualRasterPlaneDescriptor`

Kind: `Type Alias`.

```ts
type VirtualRasterPlaneDescriptor = Readonly<{ addressSpace: VirtualRasterAddressSpace; auxiliaryAxes?: readonly Readonly<{ name: string; value: string | number }>[]; channels: 1 | 2 | 3 | 4; gpuFormat: GPUTextureFormat; id: string; kind: VirtualRasterFieldKind; noData?: number; offset?: number | readonly number[]; sampleType: VirtualRasterSampleType; scale?: number | readonly number[] }>
```

### `VirtualRasterSample`

Kind: `Type Alias`.

```ts
type VirtualRasterSample = Readonly<{ physicalSlots: readonly number[]; requestedLevel: number; resolvedLodRange: readonly [number, number]; status: VirtualRasterSampleStatus; value?: readonly number[] }>
```

### `VirtualRasterSampleDescriptor`

Kind: `Type Alias`.

```ts
type VirtualRasterSampleDescriptor = Readonly<{ profile: VirtualRasterSamplingProfile; texel: readonly [number, number] }>
```

### `VirtualRasterSampleStatus`

Kind: `Type Alias`.

```ts
type VirtualRasterSampleStatus = "resident" | "fallback" | "missing" | "failed" | "no-data"
```

### `VirtualRasterSampleType`

Kind: `Type Alias`.

```ts
type VirtualRasterSampleType = "unorm8" | "float32"
```

### `virtualRasterSamplingProfile`

Kind: `Function`.

Defines level, filtering, parent fallback, and outer-boundary behavior for sampling.

```ts
Function virtualRasterSamplingProfile
```

```ts
virtualRasterSamplingProfile(descriptor: VirtualRasterSamplingProfileDescriptor): VirtualRasterSamplingProfile
```

### `VirtualRasterSamplingProfile`

Kind: `Type Alias`.

```ts
type VirtualRasterSamplingProfile = Readonly<{ filter: VirtualRasterFilter; kind: "virtual-raster-sampling-profile"; level: number; outerBoundary: VirtualRasterOuterBoundary }>
```

### `VirtualRasterSamplingProfileDescriptor`

Kind: `Type Alias`.

```ts
type VirtualRasterSamplingProfileDescriptor = Readonly<{ filter: VirtualRasterFilter; level: number; outerBoundary: VirtualRasterOuterBoundary }>
```

### `VirtualRasterSnapshot`

Kind: `Class`.

Freezes one coherent logical-to-physical page-table epoch and its physical mappings.

```ts
class VirtualRasterSnapshot
```

Members:

- `addressSpace`: `addressSpace: VirtualRasterAddressSpace` (readonly)
- `constructor`: `Constructor constructor`
  - `constructor(addressSpace: VirtualRasterAddressSpace, epoch: number, pageTable: readonly Readonly<{ contentEpoch?: number; generation?: number; physicalSlot?: number; requestedLevel: number; requestedPage: VirtualRasterPageIdentity; resolvedLevel?: number; resolvedPage?: Readonly<{ addressSpaceId: string; coordinates: readonly number[]; dimensions: CoordinateDimension; key: string; kind: "virtual-raster-page"; level: number; tile?: Readonly<{ key: string; kind: "tile-coordinate"; matrixId: string; tileCol: number; tileMatrixSetId: string; tileRow: number }> }>; status: VirtualRasterSnapshotResolveStatus }>[], physicalMappings: ReadonlyMap<number, Readonly<{ byteLength: number; channels: number; contentEpoch: number; contentVersion: string; generation: number; height: number; page: VirtualRasterPageIdentity; physicalSlot: number; width: number }>>): VirtualRasterSnapshot`
- `epoch`: `epoch: number` (readonly)
- `kind`: `kind: "virtual-raster-snapshot"` (readonly)
- `logicalGpuBytes`: `logicalGpuBytes: number` (readonly)
- `pageTable`: `pageTable: readonly Readonly<{ contentEpoch?: number; generation?: number; physicalSlot?: number; requestedLevel: number; requestedPage: VirtualRasterPageIdentity; resolvedLevel?: number; resolvedPage?: Readonly<{ addressSpaceId: string; coordinates: readonly number[]; dimensions: CoordinateDimension; key: string; kind: "virtual-raster-page"; level: number; tile?: Readonly<{ key: string; kind: "tile-coordinate"; matrixId: string; tileCol: number; tileMatrixSetId: string; tileRow: number }> }>; status: VirtualRasterSnapshotResolveStatus }>[]` (readonly)
- `residentPageCount`: `residentPageCount: number` (readonly)
- `resolve`: `Method resolve`
  - `resolve(page: VirtualRasterPageIdentity): VirtualRasterPageTableEntry`

### `VirtualRasterSnapshotResolveStatus`

Kind: `Type Alias`.

```ts
type VirtualRasterSnapshotResolveStatus = "resident" | "fallback" | "missing" | "failed"
```

### `virtualRasterTileAddressSpace`

Kind: `Function`.

Creates a compact Virtual Raster address space from finite tile-matrix coverage.

```ts
Function virtualRasterTileAddressSpace
```

```ts
virtualRasterTileAddressSpace(descriptor: VirtualRasterTileAddressSpaceDescriptor): VirtualRasterAddressSpace
```

### `VirtualRasterTileAddressSpaceDescriptor`

Kind: `Type Alias`.

```ts
type VirtualRasterTileAddressSpaceDescriptor = Readonly<{ coverage: TileMatrixCoverage; id: string }>
```

## `packages/geoscratch/src/geo/web-mercator-quad.ts`

### `GeographicPosition2D`

Kind: `Type Alias`.

```ts
type GeographicPosition2D = readonly [longitude: number, latitude: number]
```

### `ProjectedPosition2D`

Kind: `Type Alias`.

```ts
type ProjectedPosition2D = readonly [x: number, y: number]
```

### `WEB_MERCATOR_QUAD_HALF_WORLD`

Kind: `Variable`.

Projected meter distance from the central meridian to either world edge.

```ts
const WEB_MERCATOR_QUAD_HALF_WORLD: 20037508.3427892
```

### `WEB_MERCATOR_QUAD_MAX_LATITUDE`

Kind: `Variable`.

Maximum latitude representable by the Web Mercator projection.

```ts
const WEB_MERCATOR_QUAD_MAX_LATITUDE: 85.0511287798066
```

### `WEB_MERCATOR_QUAD_MAX_ZOOM`

Kind: `Variable`.

Highest matrix level defined by the built-in WebMercatorQuad model.

```ts
const WEB_MERCATOR_QUAD_MAX_ZOOM: 24
```

### `WEB_MERCATOR_QUAD_WORLD_WIDTH`

Kind: `Variable`.

Full projected Web Mercator world width in meters.

```ts
const WEB_MERCATOR_QUAD_WORLD_WIDTH: number
```

### `WebMercatorQuad`

Kind: `Variable`.

OGC-compatible WebMercatorQuad matrices with projection and tile-bound helpers.

```ts
const WebMercatorQuad: WebMercatorQuadModel
```

### `webMercatorQuadAddressCodec`

Kind: `Function`.

Creates a WebMercatorQuad address codec over finite source coverage.

```ts
Function webMercatorQuadAddressCodec
```

```ts
webMercatorQuadAddressCodec(descriptor: WebMercatorQuadAddressCodecDescriptor): WebMercatorQuadAddressCodec
```

### `WebMercatorQuadAddressCodec`

Kind: `Class`.

Encodes precise world positions and compact covered tile addresses for WebMercatorQuad.

```ts
class WebMercatorQuadAddressCodec
```

Members:

- `address`: `Method address`
  - `address(position: WebMercatorQuadPosition, matrixId: string): WebMercatorTileSampleAddress`
- `advance`: `Method advance`
  - `advance(position: WebMercatorQuadPosition, deltaQuanta: readonly [bigint, bigint]): WebMercatorQuadPosition`
- `advanceMeters`: `Method advanceMeters`
  - `advanceMeters(position: WebMercatorQuadPosition, deltaMeters: readonly [number, number]): WebMercatorQuadPosition`
- `bytesPerPosition`: `bytesPerPosition: 16` (readonly)
- `constructor`: `Constructor constructor`
  - `constructor(descriptor: WebMercatorQuadAddressCodecDescriptor): WebMercatorQuadAddressCodec`
- `coordinateBits`: `coordinateBits: number` (readonly)
- `coverage`: `coverage: TileMatrixCoverage` (readonly)
- `fromLonLat`: `Method fromLonLat`
  - `fromLonLat(position: GeographicPosition2D): WebMercatorQuadPosition`
- `fromProjected`: `Method fromProjected`
  - `fromProjected(position: ProjectedPosition2D): WebMercatorQuadPosition`
- `fromWorldQuanta`: `Method fromWorldQuanta`
  - `fromWorldQuanta(values: readonly bigint[]): WebMercatorQuadPosition`
- `positionCodec`: `positionCodec: WideFixedCodec` (readonly)
- `quantumMeters`: `quantumMeters: number` (readonly)
- `toProjected`: `Method toProjected`
  - `toProjected(position: WebMercatorQuadPosition): ProjectedPosition2D`
- `toWorldQuanta`: `Method toWorldQuanta`
  - `toWorldQuanta(position: WebMercatorQuadPosition): readonly [bigint, bigint]`
- `wgslModule`: `Method wgslModule`
  - `wgslModule(options: WebMercatorQuadAddressWgslOptions = {}): string`
- `worldQuanta`: `worldQuanta: bigint` (readonly)

### `WebMercatorQuadAddressCodecDescriptor`

Kind: `Type Alias`.

```ts
type WebMercatorQuadAddressCodecDescriptor = Readonly<{ coordinateBits?: number; coverage: TileMatrixCoverage }>
```

### `WebMercatorQuadAddressWgslOptions`

Kind: `Type Alias`.

```ts
type WebMercatorQuadAddressWgslOptions = Readonly<{ namespace?: string }>
```

### `WebMercatorQuadModel`

Kind: `Type Alias`.

```ts
type WebMercatorQuadModel = TileMatrixSet & Readonly<{ maxLatitude: number; project: any; tileBounds: any; tileFromLonLat: any; tileFromProjected: any; unproject: any }>
```

### `WebMercatorQuadPosition`

Kind: `Type Alias`.

```ts
type WebMercatorQuadPosition = Readonly<{ coordinateBits: number; fixed: WideFixedPosition; kind: "web-mercator-wide-fixed-position"; tileMatrixSetId: "WebMercatorQuad" }>
```

### `WebMercatorQuadTileBounds`

Kind: `Type Alias`.

```ts
type WebMercatorQuadTileBounds = Readonly<{ geographic: Readonly<{ east: number; north: number; south: number; west: number }>; projected: Readonly<{ east: number; north: number; south: number; west: number }> }>
```

### `WebMercatorTileSampleAddress`

Kind: `Type Alias`.

```ts
type WebMercatorTileSampleAddress = Readonly<{ compactIndex?: number; covered: boolean; kind: "web-mercator-tile-sample-address"; subTexel: readonly [number, number]; texel: readonly [number, number]; tile: TileCoordinate }>
```

## `packages/geoscratch/src/geo/web-mercator-terrain-renderer.ts`

### `createWebMercatorTerrainRenderer`

Kind: `Function`.

Assembles a WebMercatorQuad Virtual Raster terrain renderer with GPU-driven selection, precision-aware vertex generation, mesh stitching, and explicit lifetime.

```ts
Function createWebMercatorTerrainRenderer
```

```ts
createWebMercatorTerrainRenderer<ViewInput, Presentation extends string>(__namedParameters: WebMercatorTerrainRendererDescriptor<ViewInput, Presentation>): Promise<Readonly<{ stableIdentities: readonly string[]; stableIdentityFacts: WebMercatorTerrainIdentityFacts; stableIdentityHash: string; contractFacts: any; currentIdentityFacts: any; dispose: any; initialize: any; persistentFacts: any; render: any; setPresentation: any; state: any; virtualRasterFacts: any }>>
```

### `WebMercatorTerrainContractFacts`

Kind: `Type Alias`.

```ts
type WebMercatorTerrainContractFacts = Readonly<{ commandIds: Readonly<{ cover: readonly (readonly string[])[]; drawTerrain: Readonly<Record<string, readonly string[]>> }>; countPath: "gpu-produced-indirect-arguments"; cover: ReturnType<GpuWebMercatorQuadCover["facts"]>; coverMaximumMatrixLevel: number; fieldLayer: Readonly<{ demandProducerId: string; fieldId: string; id: string; representationId: string; spatialProfileId: string; viewAdapterId: string }>; passIds: Readonly<{ cover: string; terrain: string }>; persistentIdentityCount: number; selectionPath: "gpu-camera-inverse-webmercatorquad-cover"; sourceMaximumMatrixLevel: number; stageOrder: readonly string[]; terrainVertexCount: number; virtualRaster: Readonly<{ completeImageUpload: false; coordinateEncoding: WebMercatorVirtualRasterField["addressCodec"]["positionCodec"]["facts"]["encoding"]; crossPageFiltering: "logical-bilinear"; levelCount: number; maxPhysicalPages: number; pageSize: readonly number[]; sourceRevision: string }> }>
```

### `WebMercatorTerrainFrame`

Kind: `Type Alias`.

Immediate identity and provenance facts for one submitted terrain frame.

```ts
type WebMercatorTerrainFrame<Presentation extends string = string> = Readonly<{ provenance: readonly WebMercatorTerrainProvenanceFact[]; submitted: SubmittedWork; terrainPresentation: Presentation }>
```

### `WebMercatorTerrainFrameSettlement`

Kind: `Type Alias`.

Delayed inverse-cover feedback, residency work, and convergence state for one terrain frame.

```ts
type WebMercatorTerrainFrameSettlement = GeoFrameSettlement & Readonly<{ coverFeedback?: GpuWebMercatorQuadCoverFeedback; reconciliation?: VirtualRasterFeedbackReconciliation; residencySettlement: Promise<unknown>; residencyWorkCount: number; superseded: boolean }>
```

### `WebMercatorTerrainFrameValue`

Kind: `Type Alias`.

Terrain frame value paired with the exact view used for submission.

```ts
type WebMercatorTerrainFrameValue<ViewInput, Presentation extends string = string> = Readonly<{ frame: WebMercatorTerrainFrame<Presentation>; view: ViewInput }>
```

### `WebMercatorTerrainIdentityFacts`

Kind: `Type Alias`.

```ts
type WebMercatorTerrainIdentityFacts = Readonly<{ bindLayouts: number; bindSets: number; commands: number; count: number; hash: string; passes: number; pipelines: number; programs: number; resources: number; uploads: number }>
```

### `WebMercatorTerrainInitialization`

Kind: `Type Alias`.

```ts
type WebMercatorTerrainInitialization = Readonly<{ observation: Promise<WebMercatorTerrainSubmissionObservation>; submitted: SubmittedWork }>
```

### `WebMercatorTerrainPersistentFacts`

Kind: `Type Alias`.

```ts
type WebMercatorTerrainPersistentFacts = Readonly<{ bindLayouts: number; bindSets: number; logicalFootprintBytes: number; pipelines: number; resources: number }>
```

### `WebMercatorTerrainPresentationDescriptor`

Kind: `Type Alias`.

```ts
type WebMercatorTerrainPresentationDescriptor<Presentation extends string = string> = Readonly<{ fragmentEntryPoint: string; id: Presentation; label?: string }>
```

### `WebMercatorTerrainProvenanceFact`

Kind: `Type Alias`.

```ts
type WebMercatorTerrainProvenanceFact = Readonly<{ consumerStepIndex: number; declaredContentEpoch: "current-at-step"; name: string; producerContentEpoch: number; producerStepIndex: number; readContentEpoch: number; resourceId: string }>
```

### `WebMercatorTerrainRenderer`

Kind: `Type Alias`.

```ts
type WebMercatorTerrainRenderer<ViewInput, Presentation extends string = string> = Readonly<{ stableIdentities: readonly string[]; stableIdentityFacts: WebMercatorTerrainIdentityFacts; stableIdentityHash: string; contractFacts: any; currentIdentityFacts: any; dispose: any; initialize: any; persistentFacts: any; render: any; setPresentation: any; state: any; virtualRasterFacts: any }>
```

### `WebMercatorTerrainRendererDescriptor`

Kind: `Type Alias`.

```ts
type WebMercatorTerrainRendererDescriptor<ViewInput, Presentation extends string = string> = Readonly<{ elevationBounds?: readonly WebMercatorTileElevationBounds[]; elevationRangeMeters: readonly [number, number]; exaggeration?: number; fieldLayer: MapFieldLayer<ViewInput>; fieldSampling: WebMercatorTerrainSamplingWgslOptions; initialPresentation: Presentation; observeProvenance?: (facts: readonly WebMercatorTerrainProvenanceFact[]) => void; presentations: readonly WebMercatorTerrainPresentationDescriptor<Presentation>[]; presentationShader: string; runtime: GPURuntime; size: SurfaceSize; surface: Surface; variableLodPitchThresholdRadians?: number; virtualRaster: VirtualRasterRuntime<WebMercatorVirtualRasterField> }>
```

### `WebMercatorTerrainRendererState`

Kind: `Type Alias`.

```ts
type WebMercatorTerrainRendererState<Presentation extends string = string> = Readonly<{ convergenceState: "converged" | "transitioning"; coverCandidateCount: number; coverDemandCount: number; coverDemandOverflowCount: number; coverDescriptorOverflowCount: number; coverFeedback?: GpuWebMercatorQuadCoverFeedback; coverFinestMatrixLevel?: number; coverFrameEpoch?: number; coverLevelRange: readonly [number | undefined, number | undefined]; coverLookupOverflowCount: number; coverMaximumAdjacentLevelDelta: number; coverPatchCount: number; disposed: boolean; frame: number; initialized: boolean; lastResizeFacts?: WebMercatorTerrainResizeFacts; readbackInFlightCount: number; resizeGeneration: number; size: SurfaceSize; sourceLevelCeiling?: number; staleBindSetPreparationCount: number; staleFeedbackCount: number; supersededFeedbackCount: number; terrainPresentation: Presentation; virtualRequestedPageCount: number; virtualSnapshotEpoch: number }>
```

### `WebMercatorTerrainResizeFacts`

Kind: `Type Alias`.

```ts
type WebMercatorTerrainResizeFacts = Readonly<{ depthAllocationVersion: number; preparedBindSetCount: number; resizeGeneration: number; staleBindSetCount: number }>
```

### `WebMercatorTerrainSamplingWgslOptions`

Kind: `Type Alias`.

```ts
type WebMercatorTerrainSamplingWgslOptions = Readonly<{ addressNamespace: string; namespace: string; transitionTexels?: number }>
```

### `WebMercatorTerrainSubmissionObservation`

Kind: `Type Alias`.

```ts
type WebMercatorTerrainSubmissionObservation = Readonly<{ nativeStatus: "observed-succeeded"; submissionId: string }>
```

## `packages/geoscratch/src/geo/web-mercator-terrain-wgsl.ts`

### `WEB_MERCATOR_TERRAIN_TILE_WIREFRAME_FRAGMENT_ENTRY_POINT`

Kind: `Variable`.

Built-in fragment entry point for post-stitch logical-tile wireframe diagnostics.

```ts
const WEB_MERCATOR_TERRAIN_TILE_WIREFRAME_FRAGMENT_ENTRY_POINT: "WebMercatorTerrain_tile_wireframe"
```

### `webMercatorTerrainWgslModule`

Kind: `Function`.

Generates the complete precision-aware Web Mercator terrain vertex and wireframe WGSL.

```ts
Function webMercatorTerrainWgslModule
```

```ts
webMercatorTerrainWgslModule(options: WebMercatorTerrainWgslOptions): WebMercatorTerrainWgslModule
```

### `WebMercatorTerrainWgslModule`

Kind: `Type Alias`.

```ts
type WebMercatorTerrainWgslModule = Readonly<{ bindings: Readonly<{ config: number; dataGroup: number; gridPositions: number; indices: number; lookupEntries: number; mapMeta: number; sceneGroup: number; visibleInstances: number }>; code: string; kind: "web-mercator-terrain-wgsl-module"; layoutDependencies: readonly LayoutArtifact[]; namespace: string; tileWireframeFragmentEntryPoint: string; vertexEntryPoint: string }>
```

### `WebMercatorTerrainWgslOptions`

Kind: `Type Alias`.

```ts
type WebMercatorTerrainWgslOptions = Readonly<{ addressNamespace: string; cellsPerPatchEdge: number; configBinding: number; dataGroup: number; fieldNamespace: string; gridPositionsBinding: number; indicesBinding: number; lookupEntriesBinding: number; mapMetaBinding: number; namespace?: string; sceneGroup: number; visibleInstancesBinding: number }>
```

## `packages/geoscratch/src/geo/web-mercator-virtual-raster-field.ts`

### `webMercatorVirtualRasterField`

Kind: `Function`.

Composes WebMercatorQuad addressing, field semantics, coverage, and Virtual Raster storage.

```ts
Function webMercatorVirtualRasterField
```

```ts
webMercatorVirtualRasterField(descriptor: WebMercatorVirtualRasterFieldDescriptor): WebMercatorVirtualRasterField
```

### `WebMercatorVirtualRasterField`

Kind: `Type Alias`.

```ts
type WebMercatorVirtualRasterField = Readonly<{ addressCodec: WebMercatorQuadAddressCodec; addressSpace: VirtualRasterAddressSpace; coverage: TileMatrixCoverage; field: GeoField; geographicBounds: readonly [number, number, number, number]; id: string; kind: "web-mercator-virtual-raster-field"; plane: VirtualRasterPlane; representation: TiledFieldRepresentation; safetyCoverPages: readonly VirtualRasterPageIdentity[]; sourceId: string; sourceRevision: string; spatialProfile: WebMercatorPlanarTileSpatialProfile }>
```

### `WebMercatorVirtualRasterFieldDescriptor`

Kind: `Type Alias`.

```ts
type WebMercatorVirtualRasterFieldDescriptor = Readonly<{ addressSpaceId: string; auxiliaryAxes?: readonly Readonly<{ name: string; value: string | number }>[]; channels: 1 | 2 | 3 | 4; coordinateBits?: number; coverage: TileMatrixCoverage; fieldKind: VirtualRasterFieldKind; geographicBounds: readonly [number, number, number, number]; gpuFormat: GPUTextureFormat; id: string; interpolation: GeoFieldInterpolation; label?: string; noData?: number; offset?: number | readonly number[]; sampleType: VirtualRasterSampleType; scale?: number | readonly number[]; sourceRevision: string; unit?: string }>
```

## `packages/geoscratch/src/geo/web-mercator-virtual-raster-wgsl.ts`

### `webMercatorVirtualRasterWgslModule`

Kind: `Function`.

Generates precision-aware WGSL for logical Web Mercator raster sampling in any shader stage.

```ts
Function webMercatorVirtualRasterWgslModule
```

```ts
webMercatorVirtualRasterWgslModule(model: WebMercatorVirtualRasterField, options: WebMercatorVirtualRasterWgslOptions): WebMercatorVirtualRasterWgslModule
```

### `WebMercatorVirtualRasterWgslModule`

Kind: `Type Alias`.

```ts
type WebMercatorVirtualRasterWgslModule = Readonly<{ addressNamespace: string; bindings: Readonly<{ atlas: number; group: number; pageTable: number }>; code: string; kind: "web-mercator-virtual-raster-wgsl-module"; namespace: string }>
```

### `WebMercatorVirtualRasterWgslOptions`

Kind: `Type Alias`.

```ts
type WebMercatorVirtualRasterWgslOptions = Readonly<{ addressNamespace?: string; atlasBinding: number; group: number; namespace?: string; pageTableBinding: number; transitionTexels?: number }>
```
