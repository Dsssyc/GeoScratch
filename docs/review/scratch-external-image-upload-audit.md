# Scratch External Image Upload Native-Parity Audit

Status: Complete
Date: 2026-07-11
Decision: ADR-030
Native authority: [WebGPU Editor's Draft: `copyExternalImageToTexture`](https://gpuweb.github.io/gpuweb/#dom-gpuqueue-copyexternalimagetotexture)

## Result

No unresolved native-parity rows remain. Every row below has implementation and executable evidence; no row is complete from documentation alone.

| # | official/native contract | public Scratch representation | implementation location | test evidence | documentation evidence | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | The source is the current `GPUCopyExternalImageSource` union: `ImageBitmap`, `ImageData`, `HTMLImageElement`, `HTMLVideoElement`, `VideoFrame`, `HTMLCanvasElement`, or `OffscreenCanvas`. | `ExternalImageUploadCommandDescriptor.source: GPUCopyExternalImageSource` retains the source by identity and rejects arbitrary records. | `packages/geoscratch/src/scratch/command.ts` cross-realm platform getter brand checks with constructor-unavailable tag fallback. | Runtime tests exercise all seven branded source kinds and reject an unbranded dimension record; `tests/types/public-api.ts` compiles all seven canonical union members. | Vision 04 and ADR-030, Public command family and Live validation. | Complete |
| 2 | `GPUCopyExternalImageSourceInfo` exposes source, origin, and `flipY`; source pixels are captured at queue-call time. | Immutable `source`, `sourceOrigin`, and `flipY` fields; mutable source contents are application-owned. | `packages/geoscratch/src/scratch/command.ts` creates the native source info immediately before the queue call. | `tests/scratch-external-image-upload.test.js` verifies normalization, immutability, identity, live dimensions, and post-construction source mutation. | Vision 04/07 and ADR-030, Native lowering and capture time. | Complete |
| 3 | `GPUCopyExternalImageDestInfo` exposes texture, mip level, origin, aspect, color space, and premultiplied alpha. | Target resource plus `mipLevel`, `origin`, `colorSpace`, and `premultipliedAlpha`; aspect is fixed to `all`. | `packages/geoscratch/src/scratch/command.ts` builds the canonical native destination info from the current target texture. | `tests/scratch-external-image-upload.test.js` checks forwarded destination fields and fixed aspect. | Vision 04 and ADR-030, Public command family. | Complete |
| 4 | The native copy extent is one 2D layer and accepts zero width or height. | Required `{ width, height }` size; depth is fixed to `1`. | `packages/geoscratch/src/scratch/command.ts` size normalization and native `GPUExtent3D` lowering. | `tests/scratch-external-image-upload.test.js` covers integer domains, extent forwarding, and zero-area calls. | Vision 04/07 and ADR-030, Queue order and logical effects. | Complete |
| 5 | Destination must be a single-sampled 2D plain renderable color texture with `COPY_DST` and `RENDER_ATTACHMENT`; eligible formats depend on enabled features. | Construction and execution validate usage, dimension, sample count, mip/origin range, format class, and required device features. | `packages/geoscratch/src/scratch/command.ts` external-image target format table and live validation. | `tests/scratch-external-image-upload.test.js` covers base formats, gated formats, usage, dimensions, sample count, and bounds. | Vision 04 and ADR-030, Live validation. | Complete |
| 6 | Source contents are captured when `GPUQueue.copyExternalImageToTexture()` is invoked, not when a wrapper is constructed; canvas dimensions follow the bound context's output bitmap. | The command captures descriptor fields but does not snapshot pixels, call `getContext()`, or require a loaded source at construction. | `packages/geoscratch/src/scratch/command.ts` re-reads exact public image/video/frame dimensions; context-specific canvas range remains the native content-timeline check and synchronous `OperationError` maps to invalid input. | Unit tests cover post-construction mutation, all public dimension-field families, native canvas range classification, and real `examples/externalImageUpload/main.js` canvas capture after final drawing. | Vision 04/09 and ADR-030, Native lowering and Live validation. | Complete |
| 7 | External source lifetime and usability remain Web Platform and application concerns. | No Scratch resource identity, epoch, readiness state, close, dispose, fetch, decode, or playback control is assigned to the source. | `packages/geoscratch/src/scratch/command.ts` stores only the source reference and native-call inputs. | `tests/scratch-external-image-upload.test.js` verifies source identity and absence of ownership actions. | Vision 04/07 and ADR-030, Source ownership. | Complete |
| 8 | `copyExternalImageToTexture` is an immediate queue operation whose position relative to `queue.submit`, `writeBuffer`, and `writeTexture` is observable. | Fourth prepared action `{ kind: 'external-image-upload' }` in one declared submission timeline. | `packages/geoscratch/src/scratch/submission.ts` segmentation, preflight, replay, and completion registration. | `tests/scratch-submission-queue-order.test.js` covers before/between/after encoder work, mixed uploads, consecutive uploads, upload-only work, and readback staging. | Vision 05/07 and ADR-030, Queue order and logical effects. | Complete |
| 9 | A synchronous native exception means that native action did not enqueue; previous queue actions may already have succeeded. | Replay is non-retryable, stops at failure, preserves earlier committed effects, commits neither failed nor later effects, and releases readback staging for the throwing submission. | `packages/geoscratch/src/scratch/command.ts` native wrapper and `packages/geoscratch/src/scratch/submission.ts` ordered replay plus submitted/unreplayed staging cleanup. | Queue-order tests cover middle and first-action failures, later-action suppression, immediate cleanup for unreplayed staging, and post-completion cleanup for submitted staging. | Vision 05/09 and ADR-030, Native failure boundary. | Complete |
| 10 | A non-empty successful copy mutates texels; a zero-area call does not. The physical texture allocation is unchanged. | Successful non-empty execution advances target `contentEpoch` once; zero area does not; neither changes `allocationVersion`. | Shared upload-effect predicate in `packages/geoscratch/src/scratch/command.ts` and effect commit in `packages/geoscratch/src/scratch/submission.ts`. | External-image unit and submission tests assert direct/submitted epochs, zero-area behavior, and stable allocation version. | Vision 07 and ADR-030, Queue order and logical effects. | Complete |
| 11 | Logical dependency facts must describe only real target writes; the external source is not a Scratch read resource. | Non-empty success records one target write and producer epoch; zero area and failed calls record neither; no source access is invented. | `packages/geoscratch/src/scratch/submission.ts` prepared effects, access ledger, and producer ledger. | `tests/scratch-submission-queue-order.test.js` verifies resource accesses, producer epochs, readiness, dependency resolution, and zero-area absence. | Vision 05/07 and ADR-030, Queue order and logical effects. | Complete |
| 12 | Direct use still performs the same native validation and one native queue call on the owning device queue. | `ExternalImageUploadCommand.execute()` shares preflight, native write, failure wrapping, and target-effect logic with submission replay and rejects any queue other than `command.runtime.queue`. | `packages/geoscratch/src/scratch/command.ts` queue-action helpers and command execute path. | Direct tests cover success, zero area, wrong queue, invalid inputs, missing queue method, native source-range classification, and native failure cause. | Vision 04/07/09 and ADR-030. | Complete |
| 13 | WebGPU's current canonical TypeScript names are `GPUCopyExternalImageSource`, `GPUCopyExternalImageSourceInfo`, and `GPUCopyExternalImageDestInfo`. | Main and `geoscratch/scratch` entrypoints export the command and descriptor types without deprecated aliases, an optional constructor descriptor, or handwritten declarations. | `packages/geoscratch/src/scratch/index.ts`, runtime factories, generated declarations, and `@webgpu/types` 0.1.71 as the declared canonical provider. | `tests/types/public-api.ts`, provider-content contract test, package typecheck, and production declaration emission. | Vision 04 and ADR-030, Consequences. | Complete |
| 14 | Native behavior must be demonstrated with real mutable platform sources, texture sampling, and exact readback. | `examples/externalImageUpload/` uploads a cropped, `flipY` canvas region, renders the texture, and compares padded readback bytes. | `examples/externalImageUpload/main.js` plus examples catalog and Vite entry. | Chrome 150 on macOS passed desktop and 390x844 mobile runs: expected and actual rows were `[[0,0,255,255,255,255,0,255],[255,0,0,255,0,255,0,255]]`; sampled output showed blue/yellow above red/green. Post-review probes also passed a WebGL source and a `bitmaprenderer` canvas whose attributes were `7x5` while its internal output bitmap was `2x3`; both reached epoch 1 with no validation error. No page errors, failed requests, overflow, or overlap occurred. | README example tables and ADR-030. | Complete |
| 15 | A stable native capability needs synchronized API, ordering, transfer, diagnostic, decision, and contributor documentation. | The public discriminants are `commandKind: 'upload'` and `uploadKind: 'external-image'`; CPU extraction and `writeTexture()` fallback are forbidden. | Documentation contract test plus `AGENTS.md` contributor boundary. | `tests/scratch-external-image-upload-docs.test.js` checks EN/ZH parity, stable codes, ADR acceptance, example catalogs, contributor rule, and all 15 audit rows. | Vision 04/05/07/09 in English and Chinese, ADR-030, both READMEs, and this audit. | Complete |

## Audit Boundary

This audit covers texture upload through `GPUQueue.copyExternalImageToTexture()`. `GPUExternalTexture` sampled bindings, media loading/decoding, source playback, and claims of browser-level zero-copy behavior remain outside this decision.
