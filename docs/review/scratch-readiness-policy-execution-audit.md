# Scratch Readiness Policy Execution Audit

Status: Implemented evidence matrix
Date: 2026-07-10
Decision: `docs/decisions/ADR-028-scratch-readiness-policy-execution.md`

## Traceability Rules

- Public contract evidence is in `packages/geoscratch/src/scratch/command.ts` and the exports in `packages/geoscratch/src/scratch/index.ts` / `packages/geoscratch/src/index.ts`.
- Resolution evidence is in `resolveExecutableCommand()`, `resolveComputeReadiness()`, `resolveRenderReadiness()`, and `resolveSubmissionBeforeEncoding()` in `packages/geoscratch/src/scratch/submission.ts`.
- Native encoding evidence is in `DrawCommand.encode()` and `DispatchCommand.encode()` in `packages/geoscratch/src/scratch/command.ts`; resolution never duplicates those methods.
- Ledger evidence is in `SubmittedWork`, `freezeExecutionOutcomes()`, resource-access capture, and producer-epoch construction in `packages/geoscratch/src/scratch/submission.ts`.

## Eight-Row Matrix

| Command | Policy | Public Type Contract | Submission Resolution | Actual Encoder Behavior | Readiness / Epoch And Resource Ledgers | Execution Outcome | Structured Diagnostics | Automated Tests | Vision / ADR |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Draw | `throw` | `DrawCommandDescriptor` intersects `CommandReadinessDescriptor<DrawCommand>`; no fallback allowed. | `resolveExecutableCommand()` throws on the first missing read before the plan returns. | A ready command remains the requested `DrawCommand`; `DrawCommand.encode()` selects native direct/indexed/indirect render methods. | Only a selected draw applies required-epoch validation, read access, potential writes, and producer epochs. | `executed` with requested/executed ID equality and one attempt; no `SubmittedWork` exists after hard failure. | `SCRATCH_COMMAND_RESOURCE_NOT_READY`; dependency codes only when ready; command/pass/resource/submission facts included. | `scratch-pass-submission.test.js` empty/future/stale draw tests; `scratch-readiness-fallback-outcomes.test.js` ready primary; native draw tests. | `04-pipelines-commands`, `05-passes-submissions-scheduler`, `09-diagnostics-validation`, ADR-028. |
| Draw | `skip-command` | Non-fallback descriptor branch; fallback forbidden. | Missing draw resolves to omission while the pass continues. | No draw call for that request; remaining render commands and pass attachment operations keep their order. | No requested draw reads/writes/producer facts; executed render attachments still advance their own epochs. | `skipped-command` without `executedCommandId`; render pass is `executed` when attachments have effects. | Normal skip creates no diagnostic; malformed policy remains `SCRATCH_COMMAND_READINESS_POLICY_MISSING`. | `scratch-readiness-policy-execution.test.js` three-mode render attachment-side-effect loop; `examples-structure.test.js`; final browser evidence below. | `02-resources`, `04-pipelines-commands`, `05-passes-submissions-scheduler`, ADR-028. |
| Draw | `skip-pass` | Non-fallback descriptor branch; fallback forbidden. | Missing draw returns the pass trigger; render resource/query/diagnostic clones are discarded. | No render pass begins, so no load/clear/store, draw, timestamp, or occlusion encoder action occurs. | Earlier draw writes, color/depth attachment epochs, query-slot epochs, accesses, and producers are absent; later steps see the pre-pass state. | Every requested Draw is `skipped-pass`; pass has trigger ID and empty encoded IDs. | Normal pass skip creates no diagnostic; hard command/pass/resource errors are validated independently. | `scratch-readiness-policy-execution.test.js` color/depth/timestamp/occlusion and downstream attachment/query rollback tests. | `05-passes-submissions-scheduler`, `09-diagnostics-validation`, ADR-028. |
| Draw | `use-fallback` | Fallback is required and must be an actual `DrawCommand`; constructor validates runtime/lifecycle/deduplicated write-set/unique chain. | Primary missing facts are recorded, then same-position fallback attempts continue until execute/skip/throw; selected lifecycle is rechecked. | Only final selected draw reaches `DrawCommand.encode()`; static/indexed/indirect lowering stays native. | Primary reads/writes are absent; only selected fallback accesses and potential writes can create producers. | `fallback-executed` with primary requested ID, final executed ID, and complete attempts; fallback skip statuses propagate. | Contract/pass/readiness diagnostics retain requested/attempted IDs and complete available attempt snapshots. | `scratch-readiness-fallback-outcomes.test.js` draw fallback, pass incompatibility, disposed fallback, and duplicate-write facts; `examples/readinessPolicies`; constructor type/runtime tests. | `04-pipelines-commands`, `05-passes-submissions-scheduler`, `09-diagnostics-validation`, ADR-028. |
| Dispatch | `throw` | `DispatchCommandDescriptor` intersects `CommandReadinessDescriptor<DispatchCommand>`; no fallback allowed. | `resolveExecutableCommand()` hard-fails missing resources in `off`, `warn`, and `throw`. | Ready request reaches `DispatchCommand.encode()` and calls direct or indirect WebGPU dispatch. | Selected dispatch alone receives epoch diagnostics/accesses/writes/producers; static zero counts remain non-producers. | `executed` with equal IDs and a missing-empty attempt; no submitted outcome after hard failure. | `SCRATCH_COMMAND_RESOURCE_NOT_READY`; selected dependency findings use submission codes. | `scratch-submitted-work-epochs.test.js` three-mode empty dispatch and epoch tests; ready primary outcome test. | `04-pipelines-commands`, `05-passes-submissions-scheduler`, `09-diagnostics-validation`, ADR-028. |
| Dispatch | `skip-command` | Non-fallback descriptor branch; fallback forbidden. | Missing dispatch is omitted; no simulated write is committed. | No dispatch or empty compute pass is encoded when no timestamp side effect remains. | No read/write/access/producer epoch; downstream commands observe the output as empty. | Command is `skipped-command`; effect-free compute pass is `skipped-empty`. | Expected skip has no warning/error in any validation mode. | `scratch-readiness-policy-execution.test.js` three-mode omission and skipped-producer downstream tests. | `02-resources`, `04-pipelines-commands`, `05-passes-submissions-scheduler`, ADR-028. |
| Dispatch | `skip-pass` | Non-fallback descriptor branch; fallback forbidden. | Missing dispatch aborts the transaction; earlier selected dispatch state and pass diagnostics are discarded. | No compute pass begins, including when earlier dispatches appeared selectable. | No earlier/trigger reads or writes, no timestamp slot, no producer; later steps see original readiness. | All requested Dispatch outcomes are `skipped-pass`; later unattempted requests retain empty attempts; pass has trigger ID. | Normal skip has no diagnostic; unrelated hard lifecycle/ownership errors remain hard. | `scratch-readiness-policy-execution.test.js` compute rollback and stale-finding rollback; fallback-to-skip-pass test. | `05-passes-submissions-scheduler`, `09-diagnostics-validation`, ADR-028. |
| Dispatch | `use-fallback` | Fallback is required and must be an actual `DispatchCommand`; deduplicated write identity set and finite unique-ID chain enforced. | Every attempted fallback resolves its own reads/policy; optional epoch validation applies only to the final selected command. | Final fallback uses unchanged direct or `dispatchWorkgroupsIndirect` lowering; argument bytes are never host-inspected. | Primary ghosts are absent; actual fallback reads/writes and conservative indirect producer epochs identify the fallback command. | `fallback-executed`, `skipped-command`, or `skipped-pass` with stable requested/final IDs and complete attempted missing facts. | Fallback contract/pass/readiness/stale/future diagnostics retain full available attempt snapshots. | `scratch-readiness-fallback-outcomes.test.js` multi-level/skip/throw/modes/disposal/duplicate writes; `scratch-native-indirect-execution.test.js` native conservative writer. | `04-pipelines-commands`, `05-passes-submissions-scheduler`, `09-diagnostics-validation`, ADR-028. |

## Scope Check

`CopyCommand`, `ReadbackCommand`, and `ResolveQuerySetCommand` remain `whenMissing: 'throw'` only. No audit row implies otherwise. CPU-dynamic count resolvers, automatic topological scheduling, direct texture readback, expanded streaming states, and scene/layer concepts remain outside this implementation.
