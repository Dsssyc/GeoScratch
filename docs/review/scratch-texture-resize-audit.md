# Scratch Texture Resize Audit

Status: Complete
Date: 2026-07-11
Decision: ADR-031
Native authority: [WebGPU Editor's Draft: textures and texture views](https://gpuweb.github.io/gpuweb/#textures-and-texture-views)

## Result

No unresolved texture-resize rows remain. Every row below has implementation and executable evidence; no row is complete from documentation alone.

| # | official/vision contract | public Scratch representation | implementation location | test evidence | browser evidence | documentation evidence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | A logical texture may replace its physical allocation while retaining logical identity; resize is explicit resource lifecycle rather than queue work. | `TextureResource.resize(size: TextureResourceSize): void` on the same public object; exported through `geoscratch` and `geoscratch/scratch`. | `packages/geoscratch/src/scratch/texture.ts`, both Scratch entrypoints, and runtime texture factories. | `tests/scratch-texture-resize.test.js` proves stable object/id and zero encoder, queue, or completion-registration effects; `tests/types/public-api.ts` compiles both entrypoints. | `textureResize` reports unchanged resource id and explicit `texture.resize(surface.size)`. | ADR-031; Vision 01, 02, 04, 05, and 07. | Complete |
| 2 | WebGPU extent input is normalized to positive integer width, height, and depth or array layers under device limits and retained descriptor constraints. | Shared `TextureResourceSize` accepts a readonly object or readonly tuple for construction and resize. | Size normalization and deterministic allocation validation in `packages/geoscratch/src/scratch/texture.ts`. | Texture-resize tests cover both grammars, defaults, tuple length, zero, negative, fractional, `NaN`, limits, mip shrink, multisampling, array layers, and compressed-format blocks. | The proof normalizes initial `4x3x1` and replacement `8x6x1` sizes and publishes them as facts. | ADR-031, Public API and Deterministic validation; Vision 02 and 09. | Complete |
| 3 | Recreating an equivalent WebGPU texture must preserve every non-size creation field accepted by Scratch. | Read-only descriptor facts retain label, size, mip count, sample count, dimension, format, usage, `viewFormats`, and `textureBindingViewDimension`. | Descriptor normalization, frozen snapshots, and recreation forwarding in `packages/geoscratch/src/scratch/texture.ts`. | Texture-resize tests mutate caller size and `viewFormats`, inspect frozen snapshots, and compare initial and replacement native descriptors. | The browser proves a compatible real replacement; exact forwarding of fields not exposed by the page remains unit-test evidence. | ADR-031, Complete physical descriptor; Vision 02. | Complete |
| 4 | Normalized equal extents do not require a new WebGPU allocation or any logical invalidation. | Calling `resize()` with an equivalent object or tuple is a true no-op. | Same-size comparison before native creation in `packages/geoscratch/src/scratch/texture.ts`. | Texture-resize tests prove no new texture, destroy, view invalidation, version/state change, completion wait, or bind-group rebuild. | Not browser-specific; unit evidence observes native call counts and identity directly. | ADR-031, No-op and version semantics; Vision 02 and 07. | Complete |
| 5 | Replacement must exist before the old texture is removed so logical state never enters a half-installed allocation. | A changed `resize()` validates, creates, installs, then destroys. | Ordered replacement path in `TextureResource.resize()` in `packages/geoscratch/src/scratch/texture.ts`. | Texture-resize tests instrument `createTexture()` and prove the old allocation is not destroyed during creation. | The proof observes both changed physical identity and eventual old-texture destruction. | ADR-031, Create before swap; Vision 02. | Complete |
| 6 | Deterministic or synchronous native failure must leave the old allocation fully usable. | Deterministic failures use `SCRATCH_RESOURCE_DESCRIPTOR_INVALID`; synchronous native creation throws `SCRATCH_RESOURCE_ALLOCATION_REPLACEMENT_FAILED` with cause. | Validation and guarded native creation in `packages/geoscratch/src/scratch/texture.ts`; shared diagnostic envelope in `diagnostics.ts`. | Texture-resize tests snapshot all allocation facts before invalid input and forced native failure, then prove descriptor, texture, view, versions, state, and destruction remain unchanged. | Not browser-specific; injected failure is deterministic unit evidence. | ADR-031, Deterministic validation and failure atomicity; Vision 09. | Complete |
| 7 | WebGPU permits `GPUTexture.destroy()` after swap without a CPU queue-completion wait and may reclaim storage after prior submitted uses complete. | The old public `gpuTexture` identity is destroyed immediately after successful installation of the replacement. | Final step of `TextureResource.resize()` in `packages/geoscratch/src/scratch/texture.ts`. | Texture-resize tests prove destroy happens after creation and no `onSubmittedWorkDone()` registration occurs. | `textureResize` wraps the initial native texture's `destroy()` and publishes `oldGpuTextureDestroyed=true`. | WebGPU texture destruction contract; ADR-031; Vision 02 and 07. | Complete |
| 8 | Allocation identity and logical content history are independent; a replacement contains no readable content until produced. | Changed resize sets allocation version to previous plus one, preserves `contentEpoch`, and exposes `state === 'empty'`; same-size keeps all three. | Base resource replacement transition plus texture resize installation in `resource.ts` and `texture.ts`. | Texture-resize and submitted-work tests prove exact version/state transitions and the next producer epoch; pass tests prove empty replacement readiness enforcement and same-submission repopulation. | The proof reports allocation `1 -> 2`, content epoch unchanged at resize and advanced once after rendering, with `stateAfterResize=empty`. | ADR-031; Vision 02, 04, 05, and 07. | Complete |
| 9 | A `GPUTextureView` belongs to one texture allocation and cannot be reused after replacement. | `TextureResource.createView()` caches per current allocation; application-retained raw views are explicitly allocation-scoped. | Texture view cache and resize cache clearing in `packages/geoscratch/src/scratch/texture.ts`. | Texture-resize tests prove same-allocation cache reuse, post-resize view identity change, and old view association with the destroyed texture. | The visible result is rendered from the replacement view; native view identity itself is covered more directly by unit instrumentation. | ADR-031, Raw views; Vision 02 and 03. | Complete |
| 10 | Bind groups that contain a texture view must be rebuilt when the binding target changes, not when only texels change. | Persistent `BindSet` compares `allocationVersion` and lazily creates exactly one replacement bind group. | Bind-set allocation snapshots and lowering in `packages/geoscratch/src/scratch/binding.ts`. | Texture-resize and texture-sampler tests prove one rebuild after resize, reuse thereafter, and rebuilt groups in persistent draw and dispatch commands. | `textureResize` publishes `sameBindSetObject=true` while two post-resize draw executions use the replacement. | ADR-031; Vision 03 and 04. | Complete |
| 11 | Render color and depth/stencil attachments must create views from the allocation current when the pass is encoded. | Persistent `RenderPassSpec` retains logical texture resources rather than physical views. | Render-pass attachment lowering in `packages/geoscratch/src/scratch/pass.ts` and submission encoding. | Pass-submission and depth-stencil tests resize color and depth targets after pass construction, then prove views and ledger allocation versions are current. | `textureResize` publishes `samePassSpecObject=true` and visibly clears the replacement target. | ADR-031; Vision 04, 05, and 07. | Complete |
| 12 | Native uploads and all four WebGPU copy directions must use the physical resource current at execution. | Texture upload, external-image upload, texture-to-texture, buffer-to-texture, and texture-to-buffer commands retain logical resources. | Current native texture resolution in `command.ts`; submission lowering in `submission.ts`. | Texture-sampler, external-image-upload, and copy-command suites prove every target/source uses allocation version 2 after resize. | The proof renders to the replacement, performs texture-to-buffer copy, and reads the copied bytes without CPU texture extraction. | ADR-031; Vision 04, 05, and 07. | Complete |
| 13 | A command descriptor that no longer fits after shrink must fail current preflight before any encoder or queue side effect. | Stable commands are reusable only while current mip, origin, extent, layer, layout, and sample validation succeeds. | `validateCurrentRange()` and live upload/external-upload validation in `packages/geoscratch/src/scratch/command.ts`. | Copy, texture-upload, and external-image tests shrink targets or sources and prove structured failure with zero encoder, write, copy, or submit effects. | Not browser-specific; negative current-range paths use instrumented fake WebGPU call counts. | ADR-031, Downstream invalidation; Vision 04, 05, 07, and 09. | Complete |
| 14 | Submitted provenance is immutable, and host materialization must not silently reinterpret a replaced source allocation. | `SubmittedWork` freezes access/producer facts; `ReadbackOperation` captures source allocation version and rejects stale sources. | Ledger capture in `submission.ts` and allocation check in `readback.ts`. | Submitted-work tests prove allocation-1 history remains unchanged after texture replacement and same-submission writes use allocation 2; readback tests prove stale buffer rejection and that copied-buffer provenance is independent of a later texture replacement. | The proof materializes an ordered readback from the buffer produced by texture-to-buffer copy and matches exact bytes. | ADR-031; Vision 05 and 07. Direct texture readback remains outside the implemented readback surface. | Complete |
| 15 | Public declarations must express exact readonly allocation facts and current WebGPU descriptor fields without handwritten mirrors or skipped library checks. | Generated `TextureResourceDescriptor` uses the canonical descriptor minus size plus exact iterable `viewFormats` and `textureBindingViewDimension`; allocation getters and `resize()` are read-only/typed. | TypeScript source in `texture.ts`, generated `dist/scratch/texture.d.ts`, both public entrypoints, `@webgpu/types` dependency, and `tsconfig.webgpu-types.json`. | `tests/types/public-api.ts` asserts construction, resize, both exports, iterable view formats, compatibility field, and rejected assignments; `npm run typecheck` includes a strict no-emit, `skipLibCheck:false` consumer pass using TypeScript 5.9 and installed `@webgpu/types`. | Not browser-specific; declaration fidelity is compiler evidence. | ADR-031, Public API; AGENTS TypeScript source boundary. | Complete |
| 16 | A native WebGPU proof must show stable logical reuse, changed physical identity, correct versions/readiness/destruction, visible output, and exact GPU-to-buffer bytes. | Catalog entry and standalone `examples/textureResize/` page named `Texture Resize`, with machine-readable body dataset facts. | `examples/textureResize/index.html`, `main.js`, Vite input, and examples catalog. | Example-structure tests enforce package imports, explicit resize, object reuse, padded readback, dataset status, and all three legacy labels. | Real Chrome WebGPU passed at `1440x900` and `390x844`: all 17 booleans true, exact 1536 padded bytes matched, rendered center pixel was `[32,160,224]`, and there were no page errors, failed requests, overflow, or overlap. | Both root READMEs, `examples/README.md`, and ADR-031. | Complete |
| 17 | A lasting public lifecycle primitive requires synchronized decision, vision, review, contributor, example, and user documentation. | The canonical name is `TextureResource.resize()`; no legacy alias, migration flag, hidden surface coupling, or second replacement path is introduced. | Documentation contract test and repository guidance. | `tests/scratch-texture-resize-docs.test.js` checks seven EN/ZH modules, old-shape removal, stable codes, strict canonical-provider typecheck wiring, ADR acceptance, catalogs, contributor rules, and exactly 17 complete rows. | Browser evidence is linked by the example and row 16 rather than serving as documentation-only proof. | ADR-031; Vision 01/02/03/04/05/07/09 in English and Chinese; intelligent-friendly review; READMEs; examples README; AGENTS; this audit. | Complete |

## Audit Boundary

This audit covers explicit size-only replacement of persistent `TextureResource` allocations and every currently implemented downstream consumer. General descriptor reconfiguration, automatic surface tracking, direct texture `ReadbackOperation`, device-loss rehydration, persistent logical texture-view resources, and asynchronous WebGPU error-scope policy remain separate work.
