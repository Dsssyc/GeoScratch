{
  "name": "geoscratch-repo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/geoscratch",
    "examples"
  ],
  "scripts": {
    "dev": "npm --workspace examples run dev",
    "build": "npm --workspace examples run build",
    "serve": "npm --workspace examples run serve",
    "test": "mocha \"tests/**/*.test.js\""
  },
  "devDependencies": {
    "chai": "^5.1.0",
    "mocha": "^10.3.0",
    "playwright": "^1.60.0"
  }
}
